const { chromium }=require('playwright');
(async()=>{const b=await chromium.launch({channel:'chrome',headless:true});const p=await b.newPage({locale:'it-IT'});await p.goto('http://127.0.0.1:8765/tests/v7-test.html?dom='+Date.now());await p.waitForFunction(()=>document.title.startsWith('ERROR'),{timeout:120000});console.log(JSON.stringify(await p.evaluate(()=>({language:document.documentElement.lang,options:[...document.querySelector('#emojiCategory').options].map(option=>({value:option.value,text:option.textContent}))}))));await b.close();process.exit(0)})().catch(e=>{console.error(e);process.exit(1)});
