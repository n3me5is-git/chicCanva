async function runV7Tests(){const results=[],ok=(name,value)=>{results.push((value?'PASS ':'FAIL ')+name);if(!value)throw new Error(name)};try{
 localStorage.removeItem(MEMORY_KEY);localStorage.removeItem(AUTOSAVE_DIRTY_KEY);localStorage.removeItem(PREF_KEY);localStorage.removeItem('chicCanva.search-translations.v1');localStorage.setItem('chiccanva.language.v1','it');await new Promise(resolve=>{const request=indexedDB.deleteDatabase('chicCanva-storage'),done=()=>resolve();request.onsuccess=done;request.onerror=done;request.onblocked=done;setTimeout(done,800)});await init();preferences.autosave=false;clearTimeout(autosaveTimer);
 ok('v7 starts with one custom project',projects.length===1&&state.mode==='custom'&&state.pages.custom.length===1);
 ok('PWA stays disabled on localhost',document.documentElement.dataset.pwa==='off'&&$('installPwaBtn').classList.contains('hidden')&&!document.querySelector('link[rel="manifest"]'));
 ok('PWA domain gate accepts only real HTTPS domains',pwaDomainAllowed({protocol:'https:',hostname:'laboratorio.scuola.it'})&&!pwaDomainAllowed({protocol:'file:',hostname:''})&&!pwaDomainAllowed({protocol:'http:',hostname:'localhost'})&&!pwaDomainAllowed({protocol:'https:',hostname:'127.0.0.1'})&&!pwaDomainAllowed({protocol:'https:',hostname:'chiccanva.local'}));
 ok('initial locale detection considers the complete browser locale list',browserPrefersItalian(['it-IT','en-US'],'en-US','en-US','')&&!browserPrefersItalian(['en-US'],'en-US','en-GB',''));
 let pwaPromptCalls=0;deferredPwaInstall={prompt:async()=>{pwaPromptCalls++},userChoice:Promise.resolve({outcome:'accepted'})};showPwaInstructions();ok('PWA instructions expose a native install trigger when the browser supports it',$('pwaInstallDialog').open&&!$('pwaInstallConfirm').classList.contains('hidden')&&!$('pwaInstallConfirm').disabled);$('pwaInstallConfirm').click();await new Promise(resolve=>setTimeout(resolve,20));ok('PWA modal install button triggers the deferred browser prompt',pwaPromptCalls===1);deferredPwaInstall=null;showPwaInstructions();ok('PWA install dialog keeps its browser-install button visible without a deferred event',$('pwaInstallDialog').open&&!$('pwaInstallConfirm').classList.contains('hidden')&&!$('pwaInstallConfirm').disabled);$('pwaInstallDialog').close();
 ok('share metadata permits social previews while major search engines receive noindex',document.querySelector('meta[property="og:title"]')?.content.includes('Piccole idee, grandi progetti')&&document.querySelector('meta[name="twitter:card"]')?.content==='summary_large_image'&&!document.querySelector('meta[name="robots"]')&&document.querySelector('meta[name="googlebot"]')?.content==='noindex,nofollow,noarchive'&&document.querySelector('meta[name="bingbot"]')?.content==='noindex,nofollow,noarchive'&&document.querySelector('meta[property="og:image"]')?.content.startsWith('https://chiccanva.testthis.one/chiccanva-share.jpg?v=')&&document.querySelector('meta[property="og:image:type"]')?.content==='image/jpeg'&&document.querySelector('meta[property="og:image:width"]')?.content==='1200'&&document.querySelector('meta[property="og:url"]')?.content==='https://chiccanva.testthis.one/'&&document.querySelector('link[rel="canonical"]')?.href==='https://chiccanva.testthis.one/'&&!!document.querySelector('link[rel="icon"]'));
 ok('default drawing color is black',state.strokeColor==='#000000'&&$('strokeColor').value==='#000000');
 ok('duplicate JSON launcher removed',!$('openProjectBtn')&&!!$('importJsonBtn'));
 ok('Clipart explorer is a collapsible sidebar section with quick navigation',!!$('clipartCard')&&$('clipartCard').parentElement===$('sidebar')&&[...$('sidebar').querySelectorAll('.quick-jump button')].some(button=>button.textContent==='Clipart')&&$('clipartCard').querySelector(':scope>.card-h').getAttribute('role')==='button');
 const parsedClipart=parseOpenclipartResults('<a href="/detail/123/happy-child"><img alt="Happy Child"></a><a href="/detail/123/happy-child">duplicate</a><a href="https://openclipart.org/detail/456/pencil" title="School Pencil"></a>');ok('Openclipart parser extracts and deduplicates public search results',parsedClipart.length===2&&parsedClipart[0].id==='123'&&parsedClipart[0].title==='Happy Child'&&parsedClipart[1].large.endsWith('/456'));
 const manyClipart=Array.from({length:20},(_,index)=>'<a href="/detail/'+(1000+index)+'/item-'+index+'">Item '+index+'</a>').join('');ok('Openclipart limits each rendered result page to twelve previews',parseOpenclipartResults(manyClipart).length===12&&CLIPART_RESULTS_PER_PAGE===12);
 const savedClipartFetch=fetchOpenclipartSearch,remoteClipart=Array.from({length:32},(_,index)=>'<a href="/detail/'+(2000+index)+'/cached-'+index+'">Cached '+index+'</a>').join('')+'<a href="/search/?p=2&query=school">Next</a>';let clipartFetchCalls=0;fetchOpenclipartSearch=async()=>{clipartFetchCalls++;return remoteClipart};clipartPageCache.clear();const clipartFirst=await clipartItemsForPage('school',1),clipartSecond=await clipartItemsForPage('school',2);fetchOpenclipartSearch=savedClipartFetch;clipartPageCache.clear();ok('Openclipart client pagination reuses the fetched remote page',clipartFirst.items.length===12&&clipartSecond.items.length===12&&clipartSecond.items[0].id==='2012'&&clipartFetchCalls===1&&clipartSecond.hasNext);
 const localTranslation=translateClipartLocally('bambino felice a scuola'),contextualInput=searchTranslationInput('sirena (creatura mitologica)');ok('search translation has a compact offline didactic fallback and context-only parentheses',localTranslation.changed&&localTranslation.translated==='child happy school'&&translateClipartLocally('leone cuore').translated==='lion heart'&&contextualInput.query==='sirena'&&contextualInput.context==='creatura mitologica'&&JSON.stringify(SEARCH_IT_EN).length<500000);
 clearSearchTranslationCache();rememberSearchTranslation('suora (quella dei preti)','nun');ok('saved translations use only the searchable text as their key',cachedSearchTranslation('suora')==='nun'&&Object.keys(readSearchTranslationCache()).length===1&&Object.keys(readSearchTranslationCache())[0]==='suora');clearSearchTranslationCache();ok('translation cache has dedicated controls in both search widgets',!!$('clearEmojiTranslations')&&!!$('clearClipartTranslations'));
 ok('OpenMoji and Clipart translation start enabled and share the compact fallback',$('emojiTranslate').checked&&$('clipartTranslate').checked&&typeof translateEmojiKeyword==='function'&&translateClipartLocally('bambino').translated==='child');ok('translated emoji search exposes an explicit magnifier',!$('emojiSearchBtn').classList.contains('hidden')&&!!$('emojiSearchBtn').querySelector('svg'));const savedEmojiQuery=$('emojiSearch').value,savedEmojiData=emojiData;emojiData=[{annotation:'teacher',tags:'school education',openmoji_tags:'',emoji:'🧑‍🏫',hexcode:'1F9D1-200D-1F3EB',group:'people-body',subgroups:'person-role',order:1}];const rawEmojiMatches=emojiMatches().length;emojiTranslatedQuery='teacher';$('emojiSearch').value='maestra';const translatedEmojiMatches=emojiMatches();ok('translated OpenMoji keyword is applied to global metadata filtering',translatedEmojiMatches.length===1&&translatedEmojiMatches.length<=rawEmojiMatches);const savedTranslateEmojiKeyword=translateEmojiKeyword;let emojiTranslateCalls=0,releaseEmojiTranslation;translateEmojiKeyword=()=>{emojiTranslateCalls++;return new Promise(resolve=>{releaseEmojiTranslation=resolve})};$('emojiSearch').value='termine esplicito';$('emojiSearch').dispatchEvent(new Event('input',{bubbles:true}));await new Promise(resolve=>setTimeout(resolve,20));ok('typing does not launch translated emoji search',emojiTranslateCalls===0&&!emojiSearchBusy);const emojiSearchPromise=updateEmojiSearch();await new Promise(resolve=>setTimeout(resolve,50));ok('explicit emoji search stays busy during translation',emojiSearchBusy&&$('emojiGrid').hasAttribute('aria-busy')&&!!$('emojiCount').querySelector('.spinner'));releaseEmojiTranslation({translated:'teacher',note:'test'});await emojiSearchPromise;ok('emoji busy state ends after results render',!emojiSearchBusy&&$('emojiGrid').hasAttribute('aria-busy')===false&&emojiMatches().length>0);translateEmojiKeyword=savedTranslateEmojiKeyword;emojiTranslatedQuery='';emojiTranslationNote='';emojiData=savedEmojiData;$('emojiSearch').value=savedEmojiQuery;renderEmojiGrid();
 const translationPuterSaved=window.puter,translationCalls=[];window.puter={ai:{txt2img(){},chat:(prompt,options)=>{translationCalls.push({prompt,options});return Promise.resolve({message:{content:'lion cub',reasoning:'internal reasoning is ignored'}})}},auth:{isSignedIn:()=>true}};const puterTranslation=await translateSearchKeyword('cucciolo leonino raro'),cachedTranslation=await translateSearchKeyword('cucciolo leonino raro');clearTimeout(puterUsageTimer);window.puter=translationPuterSaved;ok('unknown search terms use contextual Gemma with low OpenRouter reasoning',puterTranslation.translated==='lion cub'&&puterTranslation.source==='puter'&&puterTranslation.route==='openrouter'&&translationCalls.length===1&&translationCalls[0].prompt==='"cucciolo leonino raro" -> translate IT to EN. Text in (...) is context only: use it to choose meaning, do not output it. Output translated search text only. If input is already EN, output it unchanged. Choose one best synonym. Context: emoji/clipart search for drawing, creative and educational projects.'&&translationCalls[0].options.model==='google/gemma-4-31b-it'&&translationCalls[0].options.provider==='openrouter'&&translationCalls[0].options.normalize===true&&translationCalls[0].options.reasoning?.effort==='low'&&!('reasoning_effort' in translationCalls[0].options));ok('successful AI translations persist and avoid repeated Puter calls',cachedTranslation.translated==='lion cub'&&cachedTranslation.source==='cache'&&cachedSearchTranslation('cucciolo leonino raro')==='lion cub'&&translationCalls.length===1);
 clearSearchTranslationCache();const fallbackCalls=[];window.puter={ai:{txt2img(){},chat:(prompt,options)=>{fallbackCalls.push(options);return options.provider?Promise.reject(new Error('route unavailable')):Promise.resolve({message:{content:'sparrow'}})}},auth:{isSignedIn:()=>true}};const fallbackTranslation=await translateSearchKeyword('passerotto inconsueto');clearTimeout(puterUsageTimer);window.puter=translationPuterSaved;ok('Gemma translation falls back to Puter default routing without reasoning options',fallbackTranslation.translated==='sparrow'&&fallbackTranslation.route==='default'&&fallbackCalls.length===2&&fallbackCalls[0].provider==='openrouter'&&fallbackCalls[0].reasoning?.effort==='low'&&!('provider' in fallbackCalls[1])&&!('reasoning' in fallbackCalls[1])&&!('reasoning_effort' in fallbackCalls[1]));
 const thoughtFixtures=[{message:{content:'<thought>Analizzo passero.</thought>\nsparrow'}},{message:{content:'<analysis>heart è già inglese.</analysis>\nFinal: heart'}},{message:{content:'<thought>* Input: "sirena"\n* Context check\n* Target: Mermaid.</thought>Mermaid'}},{message:{content:[{type:'text',text:'<reasoning>Check English.</reasoning>school'}]}},{message:{content:{text:'<thinking>Scelgo il contesto.</thinking>Mermaid'}}}];ok('Gemma reasoning tags are discarded and only the final translation is used',normalizedPuterText(thoughtFixtures[0])==='sparrow'&&normalizedPuterText(thoughtFixtures[1])==='heart'&&normalizedPuterText(thoughtFixtures[2])==='Mermaid'&&normalizedPuterText(thoughtFixtures[3])==='school'&&normalizedPuterText(thoughtFixtures[4])==='Mermaid');
 ok('Puter usage UI and official monthly-usage integration are present',!!$('puterUsage')&&!!$('puterUsageBar')&&typeof refreshPuterUsage==='function'&&formatPuterCredits(1000).includes('crediti')&&['puterAccountType','puterMonthlyAllowance','puterTopupCredits','puterAppUsage'].every(id=>!!$(id)));
 const billedTopup={usage:{allowanceUsed:1000},allowanceInfo:{monthUsageAllowance:1000,addons:{purchasedCredits:20000,consumedPurchaseCredits:90.34114001228214},remaining:0,unit:'credits'}},derivedTopup={allowanceInfo:{monthUsageAllowance:1000,addons:{purchasedCredits:20000},remaining:20088.899589,unit:'credits'}},unknownCapacity={allowanceInfo:{monthUsageAllowance:1000,remaining:1200,unit:'credits'}};const billedConsumed=derivePuterConsumed(billedTopup),billingUser={subscribed:false},billingSnapshot={user:billingUser,appUsage:null,unit:'credits',monthlyAllowance:1000,allowanceUsed:1000,purchasedCredits:20000,purchasedUsed:90.34114001228214,monthlyRemaining:0,purchasedRemaining:19909.65885998772,remaining:19909.65885998772,consumed:billedConsumed};ok('Puter balance math adds allowance and top-up use',Math.abs(billedConsumed.value-1090.341140012282)<.00001&&billedConsumed.source.includes('consumedPurchaseCredits')&&Math.abs(derivePuterConsumed(derivedTopup).value-911.100411)<.00001&&derivePuterConsumed(unknownCapacity).value===null&&puterFinite(null)===null);renderPuterBilling(billingSnapshot);ok('Puter balance UI renders global remaining and both bucket residuals',$('puterUsageUsed').textContent.includes('1090 crediti')&&$('puterUsageRemaining').textContent.includes('19.910')&&$('puterMonthlyAllowance').textContent.includes('Residui: 0 crediti')&&$('puterTopupCredits').textContent.includes('Usati: 90 crediti')&&parseFloat($('puterUsageBar').style.width)>94);
 ok('Puter amount conversion follows the live unit and UI rounding policy',puterAmountToUsd(2000,'credits')===1&&puterAmountToUsd(100000000,'microcents')===1&&puterCreditsLabel(911.4,'credits')==='911 crediti'&&puterUsdLabel(20088.899589,'credits',2)==='US$ 10,04'&&aiCreditText(.00588)==='12 crediti'&&aiUsdText(.00588)==='US$ 0,006');
 const livePricingFixture={models:{'openai:openai/gpt-image-2.5-flare':{costs_currency:'usd-cents',costs:{text_input:500,image_input:800,image_output:3000}},'x-ai:x-ai/grok-imagine-image':{costs_currency:'usd-cents',costs:{'output:1k':2,'output:2k':2,media_input:.2}},'byteplus:byteplus/seedream-5-0-lite-260128':{costs_currency:'usd-cents',costs:{'per-image':3.5}}}},normalizedPricing=applyLivePuterPricing(PUTER_IMAGE_PRICING_FALLBACK,livePricingFixture);ok('Puter live model catalog updates only recognized positive prices',normalizedPricing.models['openai/gpt-image-2.5-flare'].imageOutputPer1M===30&&normalizedPricing.models['x-ai/grok-imagine-image'].outputUsd['1k']===.02&&normalizedPricing.models['byteplus/seedream-5-0-lite-260128'].outputUsd===.035);
 ok('Puter image pricing exposes 30-day cache and manual refresh UI',PUTER_IMAGE_PRICING_TTL_MS===30*24*60*60*1000&&PUTER_IMAGE_PRICING_URL.includes('api.puter.com/puterai/image/models/details')&&!!$('aiPricingRefresh')&&!!$('aiPricingStatus'));
 ok('Puter account UI gates AI and provides login, switch and logout',!!$('puterAuthGate')&&!!$('puterLoginBtn')&&!!$('puterSwitchAccountBtn')&&!!$('puterLogoutBtn')&&!!$('puterLoginDialog')&&typeof requirePuterSession==='function');
 const pinchView=pinchViewport({zoom:1,distance:100},{x:20,y:30},{x:220,y:30});ok('pinch math combines zoom and moving midpoint',pinchView.zoom===2&&pinchView.mid.x===120&&pinchView.mid.y===30);const pinchDraft=new fabric.Rect({width:20,height:20});pinchDraft.excludeProject=true;canvas.add(pinchDraft);shapeMode='rect';shapeDraft=pinchDraft;shapeTrace=[{x:1,y:1}];colorizerPendingTouchOptions={e:{pointerType:'touch'}};canvasColorPendingTouchOptions={e:{pointerType:'touch'}};announceCanvasPinchStart();ok('pinch start cancels shape drafts and pending Colorizer or eyedropper touches',canvasTouchGestureBlocked()&&!shapeDraft&&shapeMode==='rect'&&!colorizerPendingTouchOptions&&!canvasColorPendingTouchOptions);canvasTouchGestureBlockedState=false;cancelShapeDrawing(false);
 const pinchObject=new fabric.Rect({left:180,top:150,width:90,height:70,fill:'#5b8',originX:'center',originY:'center'});pinchObject.objectType='shape';pinchObject.projectId=uid('obj');canvas.add(pinchObject);canvas.setActiveObject(pinchObject);canvas.requestRenderAll();canvas.calcOffset();const pinchPose={left:pinchObject.left,top:pinchObject.top},pinchUpper=canvas.upperCanvasEl,pinchRect=pinchUpper.getBoundingClientRect(),pinchCenter=pinchObject.getCenterPoint(),pinchClient={x:pinchRect.left+pinchCenter.x/canvas.width*pinchRect.width,y:pinchRect.top+pinchCenter.y/canvas.height*pinchRect.height};let leakedPinchRelease=0,leakedGhostClick=0;const countPinchRelease=()=>{leakedPinchRelease++},countGhostClick=()=>{leakedGhostClick++};pinchUpper.addEventListener('pointerup',countPinchRelease);pinchUpper.addEventListener('click',countGhostClick);const touchEvent=(type,id,x,y,buttons)=>new PointerEvent(type,{pointerId:id,pointerType:'touch',clientX:x,clientY:y,buttons,bubbles:true,cancelable:true,isPrimary:id===701});pinchUpper.dispatchEvent(touchEvent('pointerdown',701,pinchClient.x,pinchClient.y,1));pinchUpper.dispatchEvent(touchEvent('pointermove',701,pinchClient.x+14,pinchClient.y+8,1));pinchUpper.dispatchEvent(touchEvent('pointerdown',702,pinchClient.x+100,pinchClient.y,1));pinchUpper.dispatchEvent(touchEvent('pointermove',702,pinchClient.x+140,pinchClient.y+10,1));pinchUpper.dispatchEvent(touchEvent('pointerup',702,pinchClient.x+140,pinchClient.y+10,0));pinchUpper.dispatchEvent(touchEvent('pointerup',701,pinchClient.x+14,pinchClient.y+8,0));pinchUpper.dispatchEvent(new MouseEvent('click',{clientX:pinchClient.x+14,clientY:pinchClient.y+8,bubbles:true,cancelable:true}));const pinchExclusive=Math.abs(pinchObject.left-pinchPose.left)<.01&&Math.abs(pinchObject.top-pinchPose.top)<.01&&canvas.getActiveObject()===pinchObject&&!canvas._currentTransform&&leakedPinchRelease===0&&leakedGhostClick===0;pinchUpper.removeEventListener('pointerup',countPinchRelease);pinchUpper.removeEventListener('click',countGhostClick);canvas.remove(pinchObject);await new Promise(resolve=>setTimeout(resolve,380));ok('mobile pinch is exclusive, restores the prior selection and swallows tool releases and ghost clicks',pinchExclusive&&!canvasTouchGestureBlocked());
 const touchProfile=touchControlProfile(true),desktopProfile=touchControlProfile(false);ok('mobile touch controls enlarge visible handles and their invisible hit area only in the touch profile',touchProfile.cornerSize>desktopProfile.cornerSize&&touchProfile.touchCornerSize>=40&&touchProfile.padding>desktopProfile.padding&&desktopProfile.cornerSize===13&&desktopProfile.touchCornerSize===24);
const pinchWrap=$('stageWrap'),pinchStart=viewZoom,nativeSetDimensions=canvas.setDimensions,dimensionCalls=[];canvas.setDimensions=function(dimensions,options){dimensionCalls.push({dimensions,options});return nativeSetDimensions.call(this,dimensions,options)};pinchWrap.classList.add('pinching');zoomMode=String(Math.min(6,pinchStart*2));layoutCanvasViewport(Number(zoomMode),{light:true,preserve:false});const pinchApplied=viewZoom>=pinchStart*1.9&&pinchWrap.classList.contains('pinching'),pinchAvoidedRaster=dimensionCalls.length===0&&parseFloat($('canvasShell').style.getPropertyValue('--pinch-scale'))>1.5;pinchWrap.classList.remove('pinching');layoutCanvasViewport(viewZoom,{forceRaster:true});const pinchSettled=dimensionCalls.length>0&&canvas.__chicRenderScale>=1&&$('canvasShell').style.getPropertyValue('--pinch-scale')==='1';canvas.setDimensions=nativeSetDimensions;ok('pinch preview uses a transform and rebuilds the sharp raster only when the gesture ends',pinchApplied&&pinchAvoidedRaster&&pinchSettled);setZoom('2');const navigationPadding=parseFloat($('stageSpace').style.width)-parseFloat($('canvasShell').style.width);ok('hand tool has enough horizontal workspace to pan the whole zoomed canvas',navigationPadding>=pinchWrap.clientWidth*1.9&&pinchWrap.scrollWidth-pinchWrap.clientWidth>parseFloat($('canvasShell').style.width));setZoom('fit');
 const licenseFooter=document.querySelector('details.license');ok('version, build date, and transfer licenses are generated in the sidebar footer',/^v\d+\.\d+\.\d+$/.test($('appVersion').textContent)&&/^\d{4}-\d{2}-\d{2}$/.test($('appBuildDate').getAttribute('datetime'))&&licenseFooter.textContent.includes('Openclipart')&&licenseFooter.textContent.includes('CC0')&&licenseFooter.textContent.includes('PDF.js 3.11.174')&&licenseFooter.textContent.includes('Trystero 0.25.3')&&licenseFooter.textContent.includes('jsQR 1.4.0')&&licenseFooter.textContent.includes('Apache License 2.0')&&document.querySelector('.app-footer-meta .app-repository a')?.href==='https://github.com/n3me5is-git/chicCanva'&&!licenseFooter.querySelector('.app-build-info')&&!!(document.querySelector('.app-footer-meta').compareDocumentPosition(licenseFooter)&Node.DOCUMENT_POSITION_FOLLOWING));
 const deleteSvg=$('deleteTool').querySelector('svg'),deleteSvgStyle=getComputedStyle(deleteSvg);ok('toolbar edit buttons use visible SVG icons without labels',!$('copyTool').textContent.trim()&&!$('pasteTool').textContent.trim()&&!$('deleteTool').textContent.trim()&&$('deleteTool').classList.contains('iconbtn')&&!!deleteSvg.querySelector('circle')&&!!deleteSvg.querySelector('path')&&parseFloat(deleteSvgStyle.width)>=19&&deleteSvgStyle.stroke!=='none'&&deleteSvgStyle.stroke!=='rgba(0, 0, 0, 0)');
 ok('paste-special image controls are available in toolbar, image and AI widgets',!!$('pasteImageTool').querySelector('svg')&&!!$('pasteImageCanvasBtn')&&!!$('pasteAiReferenceBtn'));
 ok('export region uses a distinct area icon',!$('exportRegionTool').textContent.trim()&&!!$('exportRegionTool').querySelector('svg [stroke-dasharray]')&&$('splitTool').textContent.includes('✂'));
 const initialCards=[...$('sidebar').querySelectorAll(':scope > .card')].filter(c=>c.querySelector('.card-h'));ok('single-section mode starts with every section collapsed',preferences.accordion&&$('singleSection').checked&&initialCards.every(c=>c.classList.contains('collapsed')));
 const firstProjectId=activeProjectId;beginRename('project',firstProjectId);$('renameInput').value='Classe Arcobaleno';confirmRename();ok('project can be renamed from its tab actions',activeProject().name==='Classe Arcobaleno'&&$('projectTabs').textContent.includes('Classe Arcobaleno')&&typeof $('projectTabs').querySelector('.project-name').oncontextmenu==='function');const projectStrip=document.querySelector('.project-strip'),projectStripStyle=getComputedStyle(projectStrip),projectTabsStyle=getComputedStyle($('projectTabs')),projectNameStyle=getComputedStyle($('projectTabs').querySelector('.project-name')),projectProbe=[];for(let index=0;index<5;index++){const clone=$('projectTabs').querySelector('.project-tab').cloneNode(true);clone.querySelector('.project-name').textContent='Progetto con nome molto lungo '+index;$('projectTabs').append(clone);projectProbe.push(clone)}const workspaceWidth=document.querySelector('.workspace').getBoundingClientRect().width,toolbar=document.querySelector('.toolbar'),modebar=document.querySelector('.modebar'),barsContained=toolbar.getBoundingClientRect().width<=workspaceWidth+1&&modebar.getBoundingClientRect().width<=workspaceWidth+1&&document.documentElement.scrollWidth<=innerWidth;projectStrip.scrollLeft=projectStrip.scrollWidth;toolbar.scrollLeft=toolbar.scrollWidth;const stripRect=projectStrip.getBoundingClientRect(),createRect=$('newProjectBtn').getBoundingClientRect(),toolbarRect=toolbar.getBoundingClientRect(),lastToolRect=toolbar.lastElementChild.getBoundingClientRect(),toolbarReachable=toolbar.scrollWidth<=toolbar.clientWidth||(toolbar.scrollLeft>0&&lastToolRect.right<=toolbarRect.right+1),realScroll=projectStrip.scrollLeft>0&&createRect.left>=stripRect.left-1&&createRect.right<=stripRect.right+1&&toolbarReachable;projectProbe.forEach(node=>node.remove());projectStrip.scrollLeft=toolbar.scrollLeft=0;ok('workspace bars remain viewport-bound and really scroll to the trailing project and tool actions',projectStripStyle.overflowX==='auto'&&parseFloat(projectStripStyle.paddingBottom)>=5&&projectTabsStyle.overflow==='visible'&&parseFloat(projectNameStyle.maxWidth)<=190&&$('newProjectBtn').parentElement.classList.contains('project-strip')&&barsContained&&realScroll);
 toolbar.scrollLeft=0;const toolbarWheel=new WheelEvent('wheel',{deltaY:120,bubbles:true,cancelable:true}),toolbarOverflowStyle=getComputedStyle(toolbar);toolbar.dispatchEvent(toolbarWheel);ok('toolbar retains its native scrollbar with clearance below tools',toolbarOverflowStyle.scrollbarWidth!=='none'&&parseFloat(toolbarOverflowStyle.paddingBottom)>=10);ok('vertical mouse wheel scrolls an overflowing desktop toolbar horizontally without Shift',toolbar.scrollWidth<=toolbar.clientWidth||(toolbar.scrollLeft>0&&toolbarWheel.defaultPrevented));toolbar.scrollLeft=0;
 const resizeSetDimensions=canvas.setDimensions,resizeDimensionCalls=[],resizeOriginalZoom=zoomMode,resizeOriginalWidth=state.sidebar.width;canvas.setDimensions=function(dimensions,options){resizeDimensionCalls.push({dimensions,options});return resizeSetDimensions.call(this,dimensions,options)};zoomMode='1.5';sidebarResizeZoomMode=zoomMode;$('stageWrap').classList.add('sidebar-resizing');resizingSidebar=true;window.dispatchEvent(new PointerEvent('pointermove',{clientX:innerWidth-resizeOriginalWidth}));window.dispatchEvent(new PointerEvent('pointermove',{clientX:innerWidth-resizeOriginalWidth-20}));const resizeCoalesced=sidebarViewportFrame!==0&&resizeDimensionCalls.length===0&&zoomMode==='1.5';await new Promise(requestAnimationFrame);const resizeStayedLight=resizeDimensionCalls.length===0&&zoomMode==='1.5';window.dispatchEvent(new PointerEvent('pointerup'));const resizeSettled=resizeDimensionCalls.length>0&&zoomMode==='1.5'&&!$('stageWrap').classList.contains('sidebar-resizing');canvas.setDimensions=resizeSetDimensions;zoomMode=resizeOriginalZoom;state.sidebar.width=resizeOriginalWidth;document.documentElement.style.setProperty('--sideW',resizeOriginalWidth+'px');fitStageZoom();ok('sidebar drag preserves explicit zoom, coalesces previews and rebuilds the Fabric raster only after release',resizeCoalesced&&resizeStayedLight&&resizeSettled);
 const sidebarOpenBeforeToggle=state.sidebar.open,sidebarToggleZoomBefore=zoomMode,dynamicGestureZoom='1.137';setZoom(dynamicGestureZoom);const dynamicZoomOption=$('zoomSelect').querySelector('[data-dynamic]')?.value===dynamicGestureZoom;$('toggleSidebarBtn').click();await new Promise(resolve=>setTimeout(resolve,80));const sidebarFirstTogglePreserved=state.sidebar.open!==sidebarOpenBeforeToggle&&zoomMode===dynamicGestureZoom&&Math.abs(viewZoom-Number(dynamicGestureZoom))<.001;$('toggleSidebarBtn').click();await new Promise(resolve=>setTimeout(resolve,80));const sidebarSecondTogglePreserved=state.sidebar.open===sidebarOpenBeforeToggle&&zoomMode===dynamicGestureZoom&&Math.abs(viewZoom-Number(dynamicGestureZoom))<.001;setZoom(sidebarToggleZoomBefore);ok('opening and closing the sidebar preserves dynamic wheel or pinch zoom on desktop and mobile',dynamicZoomOption&&sidebarFirstTogglePreserved&&sidebarSecondTogglePreserved);
 const startupZoomBefore=zoomMode,startupSetDimensions=canvas.setDimensions,startupDimensionCalls=[];setZoom('1.137');canvas.setDimensions=function(dimensions,options){startupDimensionCalls.push({dimensions,options});return startupSetDimensions.call(this,dimensions,options)};beginCanvasViewportTransition();fitStageZoom();scheduleCanvasViewportLayout();scheduleCanvasViewportLayout();const startupStayedLight=startupDimensionCalls.every(call=>call.options?.cssOnly)&&zoomMode==='1.137';finishCanvasViewportTransition();await new Promise(resolve=>setTimeout(resolve,150));const startupRasterCalls=startupDimensionCalls.filter(call=>!call.options?.cssOnly).length;canvas.setDimensions=startupSetDimensions;setZoom(startupZoomBefore);ok('startup recovery transition coalesces layout changes into one final raster and preserves dynamic zoom',startupStayedLight&&startupRasterCalls===1&&zoomMode===startupZoomBefore);
 const firstPageId=state.pages.custom[0].id;beginRename('page',firstPageId);$('renameInput').value='Copertina';confirmRename();ok('page can be renamed quickly from its chip',state.pages.custom[0].name==='Copertina'&&$('pageCaption').textContent==='Copertina'&&typeof $('customPageChips').querySelector('.page-chip').oncontextmenu==='function');
 let mixed=canvas.getObjects()[0];ok('smart text and emoji form a separable group',mixed?.type==='group'&&objectTree([mixed]).some(o=>o.objectType==='text')&&objectTree([mixed]).some(o=>o.objectType==='emoji'));
canvas.setActiveObject(mixed);const mixedCenter=mixed.getCenterPoint();state.font='Fredoka';await loadFont('Fredoka');applyFontToSelectionOrFuture('Fredoka');mixed=canvas.getActiveObject();const mixedAfter=mixed.getCenterPoint(),mixedBounds=mixed.getBoundingRect(true,true);ok('font applies inside mixed text emoji group without shifting it off the page',objectTree([mixed]).filter(o=>o.objectType==='text').every(o=>o.fontFamily==='Fredoka')&&Math.hypot(mixedAfter.x-mixedCenter.x,mixedAfter.y-mixedCenter.y)<1&&mixedBounds.left>=-1&&mixedBounds.top>=-1&&mixedBounds.left+mixedBounds.width<=canvas.width+1&&mixedBounds.top+mixedBounds.height<=canvas.height+1);
 const textGroup=new fabric.Group([makeTextObj('CLASSE',-90,0,54),makeTextObj('SOLE',100,0,54)],{left:canvas.width/2,top:180,originX:'center',originY:'center',angle:8,scaleX:1.15,scaleY:1.15});textGroup.objectType='group';textGroup.projectId=uid('obj');canvas.add(textGroup);canvas.setActiveObject(textGroup);const textGroupCenter=textGroup.getCenterPoint();applyFontToSelectionOrFuture('Fredoka');const rebuiltTextGroup=canvas.getActiveObject(),rebuiltTextCenter=rebuiltTextGroup.getCenterPoint();ok('font applies to text-only groups while preserving their visual center',rebuiltTextGroup.type==='group'&&objectTree([rebuiltTextGroup]).filter(o=>o.objectType==='text').length===2&&Math.hypot(rebuiltTextCenter.x-textGroupCenter.x,rebuiltTextCenter.y-textGroupCenter.y)<1);canvas.remove(rebuiltTextGroup);canvas.setActiveObject(mixed);const logicalPoint=mixed.getCenterPoint();setZoom('4');canvas.calcOffset();mixed.setCoords();const upperRect=canvas.upperCanvasEl.getBoundingClientRect(),lowerRect=canvas.lowerCanvasEl.getBoundingClientRect(),screenEvent=new MouseEvent('mousemove',{clientX:upperRect.left+logicalPoint.x/canvas.width*upperRect.width,clientY:upperRect.top+logicalPoint.y/canvas.height*upperRect.height}),mappedPoint=canvas.getPointer(screenEvent);ok('settled high zoom keeps Fabric controls and pointer coordinates aligned',Math.abs(upperRect.width-lowerRect.width)<1&&Math.abs(upperRect.height-lowerRect.height)<1&&Math.hypot(mappedPoint.x-logicalPoint.x,mappedPoint.y-logicalPoint.y)<2&&canvas.lowerCanvasEl.width*canvas.lowerCanvasEl.height<=10100000);setZoom('fit');
 ungroupSelection();ok('ungroup exposes text and emoji separately',canvas.getActiveObject().type==='activeSelection'&&selectionObjects().some(o=>o.objectType==='text')&&selectionObjects().some(o=>o.objectType==='emoji'));
 groupSelection();mixed=canvas.getActiveObject();ok('regroup creates transformable purple group',mixed.type==='group'&&mixed.borderColor==='#7549b8'&&mixed.hasControls);
 const groupData=serializeObject(mixed),scratch=new fabric.StaticCanvas(null,{width:500,height:500});const restoredGroup=await instantiateObject(groupData,scratch);ok('group JSON round trip preserves text and emoji children',restoredGroup.type==='group'&&objectTree([restoredGroup]).some(o=>o.objectType==='text')&&objectTree([restoredGroup]).some(o=>o.objectType==='emoji'));scratch.dispose();
 copySelection();const count=canvas.getObjects().length;const savedClipboardReader=readClipboardImage;let staleImageReads=0;readClipboardImage=async()=>{staleImageReads++;return new File([new Blob(['stale'],{type:'image/png'})],'pagina-intera.png',{type:'image/png'})};await pasteSmartClipboard();readClipboardImage=savedClipboardReader;ok('smart paste prioritizes the structured chicCanva selection over a stale page image',canvas.getObjects().length===count+1&&canvas.getActiveObject().type==='group'&&staleImageReads===0);
 const structuredCount=projectClipboard.objects.length,structuredType=projectClipboard.objects[0]?.type;canvas.setActiveObject(canvas.getActiveObject());const savedClipboardWriter=writeImageBlobToClipboard;let externalPngWrites=0;writeImageBlobToClipboard=async blob=>{externalPngWrites++;return blob};await copySelectionMultipurpose();writeImageBlobToClipboard=savedClipboardWriter;ok('toolbar, context menu and keyboard share one multipurpose copy operation',projectClipboard.objects.length===structuredCount&&projectClipboard.objects[0]?.type===structuredType&&externalPngWrites===1&&$('copyTool').onclick.toString().includes('copySelectionMultipurpose'));
 const oldPage=state.pages.custom[0];insertPageAfterCurrent();await new Promise(r=>setTimeout(r,20));ok('quick page inserts after active with same format',state.pages.custom.length===2&&state.pageIndex.custom===1&&currentPage.widthMm===oldPage.widthMm&&currentPage.heightMm===oldPage.heightMm);
 await pasteSelection();ok('paste works on another page',canvas.getObjects().length===1);
 const pagesBeforeCancel=state.pages.custom.length;requestDeletePage();ok('delete page asks with an app modal',$('deletePageDialog').open&&$('deletePageInfo').textContent.includes(currentPage.name));$('deletePageCancel').click();ok('cancel keeps the page',state.pages.custom.length===pagesBeforeCancel&&!$('deletePageDialog').open);
 $('customPageName').value='Pagina laboratorio';$('customPageName').dispatchEvent(new Event('input',{bubbles:true}));ok('page name edits in realtime',currentPage.name==='Pagina laboratorio'&&$('pageCaption').textContent==='Pagina laboratorio');
 const landscape=makePage('Orizzontale',297,210);state.pages.custom.push(landscape);renderCustomChips();movePage(2,1,false);ok('page reorder changes project order',state.pages.custom[1].id===landscape.id);
 openPreview();await new Promise(r=>setTimeout(r,400));const land=$('previewGrid').querySelector('.landscape'),portrait=$('previewGrid').querySelector('.portrait');ok('preview boxes follow page orientation',!!land&&!!portrait&&land.getBoundingClientRect().width>portrait.getBoundingClientRect().width&&land.querySelector('.preview-sheet').getBoundingClientRect().width>land.querySelector('.preview-sheet').getBoundingClientRect().height);ok('preview pages expose quick rename menu',typeof land.oncontextmenu==='function'&&land.title.includes('rinominare'));ok('preview uses the standard right-aligned close icon',$('closePreview').classList.contains('iconbtn')&&$('closePreview').textContent.trim()==='×'&&$('closePreview').getAttribute('aria-label')==='Chiudi anteprima');closePreview();
 previewReorder=true;openPreview();previewSelected=1;syncPreviewArrows();$('previewMoveLeft').click();ok('preview arrows reorder selected page',state.pages.custom[0].id===landscape.id);closePreview();previewReorder=false;
 const plain=new fabric.Text("L'AMORE",{left:300,top:300,originX:'center',originY:'center',fontFamily:'Fredoka',fontSize:90,fill:'transparent',stroke:'#000',strokeWidth:3});plain.objectType='text';plain.outlineMode=true;plain.projectId=uid('obj');canvas.add(plain);canvas.setActiveObject(plain);state.symbolRules={prev:"'",next:'',smartApostrophe:true};$('symbolsPrev').value="'";$('symbolsNext').value='';$('smartApostrophe').checked=true;await splitSelectedText();ok('split obeys smart apostrophe grouping',selectionObjects().length===6&&selectionObjects()[0].objectType==='text'&&selectionObjects()[0].text==="L'");
 const beforeProjects=projects.length,blank=blankProjectPayload('Secondo');await addProject(blank,'Secondo');ok('new project opens without replacing existing',projects.length===beforeProjects+1&&activeProject().name==='Secondo');await pasteSelection();ok('clipboard crosses project boundaries',canvas.getObjects().length>0);
 const exported=currentDocument(false);await importJsonFile(new File([JSON.stringify(exported)],'importato.json',{type:'application/json'}));ok('JSON import opens a further project tab',projects.length===beforeProjects+2&&activeProject().name==='Secondo');
 $('specialText').value='questa^è una^prova';$('specialExplicit').checked=true;$('specialExplicit').dispatchEvent(new Event('change'));ok('explicit groups disable conflicting symbol rules',$('specialPrev').disabled&&$('specialApostrophe').disabled&&$('specialGroupPreview').textContent.includes('3 gruppi'));
 $('specialDestination').value='new';$('specialEmoji').checked=true;const beforeSpecial=projects.length;await generateLetterPages();ok('explicit special groups create one page per caret group',projects.length===beforeSpecial+1&&state.pages.custom.length===3&&state.pages.custom.map(p=>p.meta.group).join('|')==='questa|è una|prova');
 $('specialText').value='BENVENUTI^CIAO^I';$('specialFit').value='constant';const beforeBanner=projects.length;await generateLetterPages();const bannerScales=state.pages.custom.map(page=>page.objects[0].scaleX);ok('banner fit keeps the same character scale on every generated page',projects.length===beforeBanner+1&&state.pages.custom.every(page=>page.meta.fit==='constant')&&Math.max(...bannerScales)-Math.min(...bannerScales)<.0001&&state.pages.custom.every(page=>Math.abs(page.objects[0].left-page.widthMm*PX_PER_MM/2)<.01));
 const specialProject=activeProjectId;await activateProject(projects[0].id);ok('project switching restores independent page set',activeProjectId!==specialProject&&state.pages.custom.length===3);
 preferences.autosave=true;autosaveRevision++;await persistProject();const stored=await readMemory(),stableRecord=await dbOperation('readonly',store=>store.get(AUTOSAVE_CURRENT)),stablePayloadRecord=stableRecord?.generation?await dbOperation('readonly',store=>store.get(stableRecord.generation)):stableRecord;ok('autosave contains every open project',stored.project.workspace.projects.length===projects.length);ok('autosave stores the active project once and restores its workspace record',stored.project.workspace.activeUsesRoot&&stored.project.workspace.projects.find(record=>record.id===stored.project.workspace.activeId)?.payload?.assets&&JSON.parse(stablePayloadRecord.project).workspace.projects.find(record=>record.id===activeProjectId)?.payloadRef==='root');ok('autosave commits a stable IndexedDB generation without retaining its JSON in RAM',!!stableRecord?.generation&&!!stablePayloadRecord?.project&&!localStorage.getItem(AUTOSAVE_DIRTY_KEY)&&lastSavedPayload==='');const savedPointer=localStorage.getItem(MEMORY_KEY);localStorage.removeItem(MEMORY_KEY);const pointerless=await readMemory();localStorage.setItem(MEMORY_KEY,savedPointer);ok('autosave recovers from IndexedDB without its local pointer',pointerless.project.workspace.projects.length===projects.length);
 const closingId=projects.find(p=>p.id!==activeProjectId).id,openCount=projects.length;await closeProject(closingId,false);const afterClose=await readMemory();ok('explicit close removes project from autosave',projects.length===openCount-1&&!afterClose.project.workspace.projects.some(p=>p.id===closingId));
 askPdfScope();ok('top PDF asks export scope',$('pdfDialog').open&&$('pdfDialog').querySelectorAll('input[name="pdfScope"]').length===2);$('pdfDialog').close();
 ok('unified export defaults to complete PDF and exposes image formats',$('exportScope').value==='project'&&$('exportFormat').value==='pdf'&&$('exportClipboardBtn').classList.contains('hidden')&&['png','jpg'].every(v=>$('exportFormat').querySelector(`option[value="${v}"]`)));$('exportScope').value='current';$('exportFormat').value='png';syncExportCenter();ok('single-page PNG and JPG export offers copy to clipboard',!$('exportClipboardBtn').classList.contains('hidden')&&typeof exportImageToClipboard==='function');
 const regionProtectedObject=canvas.getObjects()[0];$('exportRegionTool').click();ok('export-area tool opens the matching export settings',$('exportScope').value==='selection'&&$('exportFormat').value==='png'&&$('cropBg').value==='transparent'&&state.regionMode&&!$('exportCard').classList.contains('collapsed'));ok('export-area drawing cannot select or move objects underneath',canvas.skipTargetFind&&regionProtectedObject&&!regionProtectedObject.selectable&&!regionProtectedObject.evented);setRegionMode(false);ok('leaving export-area mode restores object interaction',!canvas.skipTargetFind&&regionProtectedObject.selectable&&regionProtectedObject.evented);
 const zip=makeZip([{name:'a.txt',data:new TextEncoder().encode('A')}]),zipBytes=new Uint8Array(await zip.arrayBuffer());ok('page image ZIP is created client-side with a valid signature',zipBytes[0]===0x50&&zipBytes[1]===0x4b&&cleanExportName('CON')!=='CON'&&!cleanExportName('a:b?c').includes(':'));
 const cards=[...$('sidebar').querySelectorAll(':scope > .card')].filter(c=>c.querySelector('.card-h'));$('collapseSections').click();ok('collapse all closes sidebar sections',cards.every(c=>c.classList.contains('collapsed'))&&!preferences.accordion);$('expandSections').click();ok('expand all opens sidebar sections',cards.every(c=>!c.classList.contains('collapsed')));$('singleSection').checked=true;$('singleSection').dispatchEvent(new Event('change'));$('sidebar').querySelector('.quick-jump button:nth-child(6)').click();ok('single-section navigation opens only target card',cards.filter(c=>!c.classList.contains('collapsed')).length===1);
 ok('page name and nav both expose add control',!!$('quickAddNav')&&!!$('customPageName'));
 ok('context menu includes copy paste group split and position lock', ['copy','paste','group','ungroup','split','lock'].every(a=>document.querySelector(`#objectMenu [data-action="${a}"]`)));ok('layer arrows expose one-step and absolute context commands while the canvas menu stays absolute',$('moveAboveMenu').querySelector('[data-layer-action="above"]')&&$('moveAboveMenu').querySelector('[data-layer-action="top"]')&&$('moveBelowMenu').querySelector('[data-layer-action="below"]')&&$('moveBelowMenu').querySelector('[data-layer-action="bottom"]')&&document.querySelector('#objectMenu [data-action="front"]').textContent.includes('cima')&&document.querySelector('#objectMenu [data-action="back"]').textContent.includes('fondo'));
 ok('shapes are categorized, paginated and placed before images',!!$('shapesCard')&&$('shapesCard').closest('#sidebar')&&[...document.querySelectorAll('.quick-jump button')].findIndex(button=>button.textContent.trim()==='Forme')<[...document.querySelectorAll('.quick-jump button')].findIndex(button=>button.textContent.trim()==='Immagini')&&$('shapeTools').querySelectorAll('[data-shape]').length>=40&&$('shapeTabs').querySelectorAll('[data-shape-filter]').length===5);
 const shapeDraftTest=createShapeDraft('star',{x:30,y:40},{x:170,y:180});shapeDraftTest.excludeProject=false;canvas.add(shapeDraftTest);const shapeData=serializeObject(shapeDraftTest),shapeScratch=new fabric.StaticCanvas(null,{width:240,height:240}),shapeRoundTrip=await instantiateObject(shapeData,shapeScratch);ok('vector shapes survive project JSON with editable geometry and style',shapeData.objectType==='shape'&&shapeData.geometry.points.length===10&&shapeRoundTrip.objectType==='shape'&&shapeRoundTrip.type==='polygon'&&shapeRoundTrip.stroke==='#000000');shapeScratch.dispose();canvas.remove(shapeDraftTest);
 const interactionProbe=canvas.getObjects()[0];beginShapeMode('rect');ok('shape drawing isolates existing canvas objects',shapeMode==='rect'&&canvas.skipTargetFind&&!canvas.selection&&interactionProbe&&!interactionProbe.selectable&&!interactionProbe.evented);cancelShapeDrawing(false);ok('leaving shape drawing restores canvas interaction',!shapeMode&&!canvas.skipTargetFind&&interactionProbe.selectable&&interactionProbe.evented);
 const editableShape=createShapeDraft('star',{x:60,y:60},{x:180,y:180});editableShape.excludeProject=false;editableShape.selectable=true;editableShape.evented=true;canvas.add(editableShape);canvas.setActiveObject(editableShape);toggleShapePointEdit();canvas.discardActiveObject();selectionChanged();ok('shape point editing persists while the sidebar or canvas focus changes',shapeEditing===editableShape&&canvas.getActiveObject()===editableShape&&$('shapeEditPoints').classList.contains('active')&&Object.keys(editableShape.controls).length===editableShape.points.length);const firstShapePoint={...editableShape.points[0]},firstControl=editableShape.controls.p0,firstControlPosition=polygonPositionHandler.call(firstControl,null,null,editableShape),pointMoved=polygonPointAction.call(firstControl,null,{target:editableShape,corner:'p0'},firstControlPosition.x+18,firstControlPosition.y+11);ok('shape edit nodes move polygon vertices on desktop and touch',pointMoved&&(Math.abs(editableShape.points[0].x-firstShapePoint.x)>1||Math.abs(editableShape.points[0].y-firstShapePoint.y)>1));toggleShapePointEdit();
 state.snap.enabled=true;state.snap.rotationStep=10;editableShape.set('angle',16);canvas.fire('object:rotating',{target:editableShape});ok('snap applies the configured degree step to rotation',editableShape.angle===20&&$('snapRotationStep').value==='10');transformActiveSelection('right');transformActiveSelection('flip-h');ok('rotation and flip context actions transform the active object',editableShape.angle===110&&editableShape.flipX&&$('rotationMenu').querySelectorAll('[data-transform]').length===6&&$('objectTransformMenu').querySelectorAll('[data-transform]').length===6);state.snap.enabled=false;
 const guideA=new fabric.Rect({left:80,top:90,width:30,height:30,originX:'center',originY:'center'}),guideB=new fabric.Rect({left:120,top:90,width:30,height:30,originX:'center',originY:'center'});guideA.projectId=uid('obj');guideB.projectId=uid('obj');canvas.add(guideA,guideB);const guideSelection=new fabric.ActiveSelection([guideA,guideB],{canvas});guideSelection.setPositionByOrigin(new fabric.Point(canvas.width/2+5,140),'center','center');guideSelection.setCoords();state.guideSnap={enabled:true,page:true,objects:false,spacing:false};applyAlignmentGuides({target:guideSelection,e:{altKey:false,shiftKey:false}});const guideBounds=objectBounds(guideSelection);ok('magnetic guides center a complete multi-selection and render temporary non-project lines',Math.abs(guideBounds.cx-canvas.width/2)<.1&&alignmentGuideObjects.length===1&&alignmentGuideObjects.every(line=>line.excludeProject&&line.excludeFromExport));clearAlignmentGuides();canvas.discardActiveObject();canvas.remove(guideA,guideB);
 const aspectProbe=new fabric.Rect({left:100,top:100,width:80,height:40,scaleX:1.6,scaleY:.7});state.aspectLock=true;rememberAlignmentTransform({target:aspectProbe,transform:{original:{scaleX:1,scaleY:1},action:'scaleX',corner:'mr'}});const aspectAnchor={...alignmentTransform.anchor.point};constrainAspectRatio({target:aspectProbe,e:{shiftKey:false},transform:{action:'scaleX',corner:'mr'}});const lockedAspect=aspectProbe.scaleX===aspectProbe.scaleY,anchoredAspect=aspectProbe.getPointByOrigin(alignmentTransform.anchor.originX,alignmentTransform.anchor.originY);aspectProbe.set({scaleX:1.6,scaleY:.7});rememberAlignmentTransform({target:aspectProbe,transform:{original:{scaleX:1,scaleY:1},action:'scaleX',corner:'mr'}});constrainAspectRatio({target:aspectProbe,e:{shiftKey:true},transform:{action:'scaleX',corner:'mr'}});ok('aspect lock applies to side-handle scaling without moving its opposite edge and Shift temporarily inverts it',lockedAspect&&Math.hypot(anchoredAspect.x-aspectAnchor.x,anchoredAspect.y-aspectAnchor.y)<.01&&aspectProbe.scaleX===1.6&&aspectProbe.scaleY===.7&&!!$('aspectRatioLock')&&!!$('snapOptionsMenu'));state.aspectLock=false;state.guideSnap={enabled:true,page:true,objects:true,spacing:true};syncAlignmentUi();
 const resizeProbe=new fabric.Rect({left:120,top:140,width:80,height:50,originX:'left',originY:'top'});resizeProbe.projectId=uid('obj');canvas.add(resizeProbe);state.snap.enabled=true;state.snap.spacingMm=10;state.guideSnap.enabled=false;rememberAlignmentTransform({target:resizeProbe,transform:{original:{scaleX:1,scaleY:1},action:'scale',corner:'br'}});const resizeAnchor={...alignmentTransform.anchor.point};resizeProbe.set({scaleX:1.24,scaleY:1.31});applyResizeSnapping({target:resizeProbe,e:{},transform:{action:'scale',corner:'br'}});const resizedAnchor=resizeProbe.getPointByOrigin('left','top'),resizedBounds=objectBounds(resizeProbe),gridPx=10*PX_PER_MM;ok('classic snap applies while resizing and keeps the opposite corner fixed',Math.hypot(resizedAnchor.x-resizeAnchor.x,resizedAnchor.y-resizeAnchor.y)<.01&&Math.abs(resizedBounds.right/gridPx-Math.round(resizedBounds.right/gridPx))<.02&&Math.abs(resizedBounds.bottom/gridPx-Math.round(resizedBounds.bottom/gridPx))<.02);resizeProbe.set({left:220,top:190,scaleX:2,scaleY:.5});cancelActiveTransform();ok('Escape transform rollback restores the complete pose before commit',Math.abs(resizeProbe.left-120)<.01&&Math.abs(resizeProbe.top-140)<.01&&Math.abs(resizeProbe.scaleX-1)<.01&&Math.abs(resizeProbe.scaleY-1)<.01);resizeProbe.set({left:120,top:140,scaleX:1,scaleY:1});state.aspectLock=false;state.snap.enabled=true;rememberAlignmentTransform({target:resizeProbe,transform:{original:{scaleX:1,scaleY:1},action:'scale',corner:'br'}});resizeProbe.set({scaleX:1.24,scaleY:1.24});applyResizeSnapping({target:resizeProbe,e:{},transform:{action:'scale',corner:'br'}});ok('corner resize keeps its current aspect ratio while snapping to guides or grid',Math.abs(resizeProbe.scaleX-resizeProbe.scaleY)<.001);canvas.remove(resizeProbe);state.snap.enabled=false;state.guideSnap={enabled:true,page:true,objects:true,spacing:true};
 $('shapeLockSquare').checked=true;globalThis.alignmentShapeShift=false;const squareBox=constrainShapeBox('rect',{x:0,y:0},{x:90,y:40});globalThis.alignmentShapeShift=true;const freeBox=constrainShapeBox('rect',{x:0,y:0},{x:90,y:40});globalThis.alignmentShapeShift=false;$('shapeLockSquare').checked=false;ok('shape 1:1 lock is separate and Shift reverses it for the current drawing gesture',squareBox.width===squareBox.height&&freeBox.width===90&&freeBox.height===40);
 canvas.setActiveObject(editableShape);const rasterSourceCount=canvas.getObjects().length;await duplicateSelectionAsImage();ok('any renderable object can be duplicated as a croppable image',canvas.getObjects().length===rasterSourceCount+1&&canvas.getActiveObject().objectType==='image'&&state.assets[canvas.getActiveObject().assetId]?.kind==='shape-raster');canvas.remove(canvas.getActiveObject());canvas.remove(editableShape);
 ok('copy toolbar exposes copy-as-image for objects, groups and multiselections',typeof copySelectionAsImageToClipboard==='function'&&$('copyImageMenu').querySelector('[data-copy-image="selection"]'));
 ok('Colorizer is placed after images with paint, gradients, sixteen textures, brush and clipboard controls',$('colorizerCard').previousElementSibling===$('uploadImageBtn').closest('.card')&&[...$('sidebar').querySelectorAll('.quick-jump button')].findIndex(button=>button.textContent.trim()==='Colorizer')>[...$('sidebar').querySelectorAll('.quick-jump button')].findIndex(button=>button.textContent.trim()==='Immagini')&&$('colorizerTextures').querySelectorAll('[data-colorizer-texture]').length===16&&!!$('copyColorizedBtn')&&!!$('copyColorizerOriginalBtn')&&$('colorizerRasterScale').options[0].value==='1'&&$('colorizerRasterScale').value==='2');
const fringeBase=new Uint8ClampedArray([0,0,0,72]),fringeOutput=new Uint8ClampedArray(fringeBase);blendColorizerPixel(fringeOutput,fringeBase,0,[143,63,163],100);const edgeRendered=new Uint8ClampedArray([255,255,255,0,0,0,0,96,0,0,0,255,255,255,255,0]),edgePoints=new Int32Array(4),edgeAccepted=new Uint8Array(4),savedColorizerSession=colorizerSession;edgePoints[0]=0;edgeAccepted[0]=1;colorizerSession={labels:new Uint32Array(4)};const edgeCount=expandColorizerFillToEdge(edgePoints,1,edgeAccepted,edgeRendered,[255,255,255,0],32,4,1);colorizerSession=savedColorizerSession;ok('Colorizer paints behind antialias and stops edge adhesion at the opaque contour',fringeOutput[3]===255&&fringeOutput[0]>0&&edgeCount===3&&edgeAccepted[1]===1&&edgeAccepted[2]===2&&!edgeAccepted[3]);const colorSurface=document.createElement('canvas');colorSurface.width=colorSurface.height=24;const colorContext=colorSurface.getContext('2d');colorContext.fillStyle='#000';colorContext.fillRect(0,0,24,24);colorContext.fillStyle='#fff';colorContext.fillRect(3,3,18,18);const colorAssetId=uid('asset'),colorUrl=colorSurface.toDataURL('image/png');state.assets[colorAssetId]={id:colorAssetId,dataUrl:colorUrl,originalDataUrl:colorUrl,mime:'image/png',name:'Colorizer test',kind:'upload',source:'test',meta:{}};const colorOriginal=await addImageAsset(colorAssetId,'image');canvas.setActiveObject(colorOriginal);await startColorizer();const colorObject=colorizerSession?.object,cw=colorizerSession.surface.width,ch=colorizerSession.surface.height,cx=Math.floor(cw/2),cy=Math.floor(ch/2);$('colorizerColorA').value='#ff0000';$('colorizerPaintType').value='solid';const filled=colorizerFloodFill(cx,cy);await commitColorizerSurface('fill');const colorizedId=colorObject.assetId,colorMeta=colorizerMeta(state.assets[colorizedId]),fillPixels=colorizerSession.context.getImageData(0,0,cw,ch).data,centerIndex=(cy*cw+cx)*4,borderIndex=(2*cw+2)*4;ok('Colorizer keeps an immutable base and fills a closed region',filled&&colorObject.objectType==='colorized'&&colorMeta?.sourceDescriptors?.[0]?.assetId===colorAssetId&&colorMeta.baseAssetId&&state.assets[colorMeta.baseAssetId]&&colorMeta.operations.at(-1).tool==='fill'&&fillPixels[centerIndex]>240&&fillPixels[centerIndex+1]<20&&fillPixels[borderIndex]<20);const transparentBase=new Uint8ClampedArray(4),transparentOutput=new Uint8ClampedArray(4);blendColorizerPixel(transparentOutput,transparentBase,0,[255,0,0],100);ok('Colorizer paints transparent interiors of outlined text',transparentOutput[0]===255&&transparentOutput[3]===255);$('colorizerColorA').value='#0000ff';$('colorizerBrushSize').value='18';$('colorizerTolerance').value='35';$('colorizerSmartEdges').checked=true;const boundary=colorizerSession.context.getImageData(0,0,cw,ch).data,layerId=nextColorizerLayerId();colorizerSession.layers.push({id:layerId,style:{...currentColorizerStyle(),centerX:cx,centerY:cy}});const smartSeed=[colorizerSession.baseImage.data[centerIndex],colorizerSession.baseImage.data[centerIndex+1],colorizerSession.baseImage.data[centerIndex+2],colorizerSession.baseImage.data[centerIndex+3]],smartBounds={minX:cw,minY:ch,maxX:-1,maxY:-1,dirtyMinX:cw,dirtyMinY:ch,dirtyMaxX:-1,dirtyMaxY:-1};colorizerStroke={beforeLabels:colorizerSession.labels.slice(),beforeLayers:cloneData(colorizerSession.layers),seed:smartSeed,last:{x:cx,y:cy},layerId,points:new Set(),newPoints:new Set(),touchedLabels:new Set(),bounds:smartBounds,allowed:colorizerBrushAllowedMask(cy*cw+cx,smartSeed)};colorizerBrushStamp(cx,cy);colorizerBrushStamp(1,cy);renderColorizerSurface();const brushed=colorizerSession.context.getImageData(0,0,cw,ch).data;ok('smart Colorizer brush stays in its starting compartment and merges when begun over an existing colorize',brushed[centerIndex+2]>240&&brushed[borderIndex]<20&&brushed[borderIndex+2]<20&&colorizerStroke.touchedLabels.has(1));const labelsBeforeFree=colorizerSession.labels.slice(),layersBeforeFree=cloneData(colorizerSession.layers),freeLayer=nextColorizerLayerId();$('colorizerSmartEdges').checked=false;colorizerSession.layers.push({id:freeLayer,style:currentColorizerStyle()});colorizerStroke={layerId:freeLayer,points:new Set(),newPoints:new Set(),touchedLabels:new Set(),bounds:{minX:cw,minY:ch,maxX:-1,maxY:-1,dirtyMinX:cw,dirtyMinY:ch,dirtyMaxX:-1,dirtyMaxY:-1},allowed:new Uint8Array(cw*ch)};colorizerBrushStamp(2,2);ok('brush without edge protection paints continuously over outlines',colorizerSession.labels[2*cw+2]===freeLayer&&colorizerStroke.points.has(2*cw+2));colorizerSession.labels=labelsBeforeFree;colorizerSession.layers=layersBeforeFree;$('colorizerSmartEdges').checked=true;const topologyLabels=colorizerSession.labels.slice(),topologyLayers=cloneData(colorizerSession.layers),regionA=nextColorizerLayerId();colorizerSession.layers.push({id:regionA,style:currentColorizerStyle()});const regionB=nextColorizerLayerId();colorizerSession.layers.push({id:regionB,style:currentColorizerStyle()});const joiningRegion=nextColorizerLayerId();colorizerSession.layers.push({id:joiningRegion,style:currentColorizerStyle()});colorizerSession.labels[0]=regionA;colorizerSession.labels[10]=regionB;colorizerSession.labels[1]=joiningRegion;mergeColorizerLayers(joiningRegion,new Set([regionA]));ok('equal Colorizer styles merge only when their regions touch',colorizerSession.labels[0]===joiningRegion&&colorizerSession.labels[10]===regionB);colorizerSession.labels=topologyLabels;colorizerSession.layers=topologyLayers;colorizerStroke=null;const optimizedBefore=colorizerSession.labels.slice(),optimizedLayer=nextColorizerLayerId(),optimizedLength=cw*ch,optimizedBounds={minX:cw,minY:ch,maxX:-1,maxY:-1,dirtyMinX:cw,dirtyMinY:ch,dirtyMaxX:-1,dirtyMaxY:-1};colorizerSession.layers.push({id:optimizedLayer,style:currentColorizerStyle()});colorizerStroke={engine:'optimized',beforeLayers:cloneData(colorizerSession.layers.slice(0,-1)),seed:smartSeed,last:{x:cx,y:cy},layerId:optimizedLayer,points:colorizerIndexBuffer(),previousLabels:colorizerIndexBuffer(),newPoints:colorizerIndexBuffer(),pointMask:new Uint8Array(optimizedLength),dirtyMask:new Uint8Array(optimizedLength),touchedLabels:new Set(),samples:[],bounds:optimizedBounds,allowed:colorizerBrushAllowedMask(cy*cw+cx,smartSeed)};colorizerBrushStamp(cx,cy);colorizerBrushLine({x:cx,y:cy},{x:cx+1,y:cy});colorizerProcessBrushSamples();renderColorizerBrushPreview();const optimizedPreviewVisible=!!colorizerSession.liveOverlay&&colorizerStroke.points.data instanceof Uint32Array&&!colorizerStroke.points.size;cancelColorizerStroke();ok('optimized Colorizer brush uses typed pixel buffers, live edge-aware overlay and atomic cancellation',optimizedPreviewVisible&&!colorizerSession.liveOverlay&&colorizerSession.labels.every((value,index)=>value===optimizedBefore[index]));const dependencyPayload={pages:{custom:[{objects:[serializeObject(colorObject)]}]},history:{entries:[]},assets:state.assets},dependencyIds=collectPayloadAssetIds(dependencyPayload,true);ok('Colorizer source assets remain reachable through derived metadata and project export',dependencyIds.has(colorizedId)&&dependencyIds.has(colorAssetId)&&!!referencedAssets([{pages:{custom:[{objects:[serializeObject(colorObject)]}]}}])[colorAssetId]);stopColorizer();ok('Colorizer result remains a normal image after leaving the mode',selectedImage()===colorObject);await restoreColorizerOriginal();ok('Colorizer restores the editable source and releases its temporary raster',canvas.getActiveObject()?.objectType==='image'&&canvas.getActiveObject()?.assetId===colorAssetId&&!colorizerSession);canvas.remove(canvas.getActiveObject());const groupParts=[makeTextObj('A',0,0,44),makeTextObj('B',48,0,44)],colorGroup=new fabric.Group(groupParts,{left:260,top:260,originX:'center',originY:'center'});colorGroup.objectType='group';colorGroup.projectId=uid('obj');canvas.add(colorGroup);canvas.setActiveObject(colorGroup);await startColorizer();const groupColorMeta=colorizerMeta(state.assets[colorizerSession.object.assetId]);stopColorizer();await restoreColorizerOriginal();ok('Colorizer supports groups and restores their editable children',groupColorMeta?.sourceDescriptors?.[0]?.objectType==='group'&&canvas.getActiveObject()?.objectType==='group'&&canvas.getActiveObject().getObjects().length===2);canvas.remove(canvas.getActiveObject());
 const outlineText=new fabric.Text('O',{left:340,top:260,originX:'center',originY:'center',fontFamily:'Arial',fontSize:96,fill:'rgba(0,0,0,0)',stroke:'#000000',strokeWidth:4,paintFirst:'stroke',objectCaching:false});outlineText.objectType='text';outlineText.outlineMode=true;outlineText.projectId=uid('obj');canvas.add(outlineText);canvas.setActiveObject(outlineText);await startColorizer();const outlineSession=colorizerSession,outlineX=Math.floor(outlineSession.surface.width/2),outlineY=Math.floor(outlineSession.surface.height/2),outlinePoint=outlineY*outlineSession.surface.width+outlineX;$('colorizerColorA').value='#8f3fa3';$('colorizerPaintType').value='solid';const outlinedFilled=colorizerFloodFill(outlineX,outlineY),outlinePixel=outlineSession.context.getImageData(outlineX,outlineY,1,1).data;openColorizerMenu(80,80,{x:outlineX,y:outlineY});ok('actual outlined text can be filled and exposes the Colorize removal menu',outlinedFilled&&outlineSession.labels[outlinePoint]>0&&outlinePixel[3]>0&&!$('colorizerContextMenu').classList.contains('hidden'));$('colorizerContextMenu').querySelector('[data-colorizer-action="erase"]').click();await new Promise(resolve=>setTimeout(resolve,120));ok('Colorize context command removes the connected region',outlineSession.labels[outlinePoint]===0);const pendingKeeper=uid('asset');state.assets[pendingKeeper]={id:pendingKeeper,dataUrl:'data:image/png;base64,AA==',mime:'image/png',name:'pending',meta:{}};outlineSession.pendingAssetId=pendingKeeper;pruneUnreachableAssets();ok('asset cleanup preserves active Colorizer originals and pending commits',!!state.assets[pendingKeeper]&&!!state.assets[outlineSession.meta.baseAssetId]);outlineSession.pendingAssetId=null;delete state.assets[pendingKeeper];stopColorizer();await restoreColorizerOriginal();ok('outlined text original remains recoverable after Colorizer',canvas.getActiveObject()?.objectType==='text'&&canvas.getActiveObject()?.text==='O');canvas.remove(canvas.getActiveObject());
 openColorizerSection();$('uploadImageBtn').closest('.card').querySelector(':scope > .card-h').click();ok('Colorizer follows single-section sidebar collapsing',$('colorizerCard').classList.contains('collapsed')&&getComputedStyle($('colorizerCard').querySelector(':scope > .card-b')).display==='none'&&!$('uploadImageBtn').closest('.card').classList.contains('collapsed'));
 const moveA=new fabric.Rect({left:120,top:120,width:20,height:20}),moveB=new fabric.Rect({left:170,top:120,width:20,height:20});for(const object of [moveA,moveB]){object.projectId=uid('obj');canvas.add(object)}canvas.discardActiveObject();startSelectionTapMode();selectionTapSet=new Set([moveB,moveA]);rebuildTapSelection();ok('tap selection rebuilds in canvas stacking order instead of touch order',canvas.getActiveObject().getObjects()[0]===moveA&&canvas.getActiveObject().getObjects()[1]===moveB);const movableSelection=canvas.getActiveObject(),selectionLeft=movableSelection.left;canvas.fire('mouse:down',{target:movableSelection,e:{}});scheduleAutosave();await new Promise(resolve=>setTimeout(resolve,420));const sameSelectionDuringDrag=canvas.getActiveObject()===movableSelection;movableSelection.set('left',selectionLeft+30);movableSelection.setCoords();canvas.fire('object:modified',{target:movableSelection});canvas.fire('mouse:up',{target:movableSelection,e:{}});ok('multiple selected objects keep moving while delayed autosave waits for pointer release',selectionTapMode&&sameSelectionDuringDrag&&movableSelection.type==='activeSelection'&&!movableSelection.lockMovementX&&movableSelection.left===selectionLeft+30&&$('selectionModeMenu').querySelector('[data-selection-tap="stop"]'));stopSelectionTapMode(false);canvas.remove(moveA,moveB);const layerA=new fabric.Rect({width:10,height:10}),layerB=new fabric.Rect({width:10,height:10}),layerC=new fabric.Rect({width:10,height:10}),layerD=new fabric.Rect({width:10,height:10});for(const object of [layerA,layerB,layerC,layerD]){object.projectId=uid('obj');canvas.add(object)}canvas.setActiveObject(new fabric.ActiveSelection([layerC,layerB],{canvas}));groupSelection();const orderedGroup=canvas.getActiveObject();ok('grouping preserves the canvas layer order even when selection order is reversed',orderedGroup.type==='group'&&orderedGroup.getObjects()[0]===layerB&&orderedGroup.getObjects()[1]===layerC);ungroupSelection();let layerOrder=canvas.getObjects().filter(object=>[layerA,layerB,layerC,layerD].includes(object));ok('ungroup keeps the selected children together at their resolved stack position',layerOrder[0]===layerA&&layerOrder[1]===layerD&&layerOrder[2]===layerB&&layerOrder[3]===layerC);moveSelectionLayer('below');layerOrder=canvas.getObjects().filter(object=>[layerA,layerB,layerC,layerD].includes(object));const movedBelow=layerOrder[0]===layerA&&layerOrder[1]===layerB&&layerOrder[2]===layerC&&layerOrder[3]===layerD;moveSelectionLayer('above');layerOrder=canvas.getObjects().filter(object=>[layerA,layerB,layerC,layerD].includes(object));ok('one-step multi-object layer movement preserves relative order in both directions',movedBelow&&layerOrder[0]===layerA&&layerOrder[1]===layerD&&layerOrder[2]===layerB&&layerOrder[3]===layerC);canvas.discardActiveObject();canvas.remove(layerA,layerB,layerC,layerD);const dragGroup=new fabric.Group([new fabric.Rect({left:0,top:0,width:20,height:20}),new fabric.Circle({left:30,top:0,radius:10})],{left:180,top:180});dragGroup.objectType='group';dragGroup.projectId=uid('obj');canvas.add(dragGroup);canvas.setActiveObject(dragGroup);canvas.fire('mouse:down',{target:dragGroup,e:{}});scheduleAutosave();await new Promise(resolve=>setTimeout(resolve,420));const sameGroupDuringDrag=canvas.getActiveObject()===dragGroup;canvas.fire('mouse:up',{target:dragGroup,e:{}});ok('Fabric groups also survive autosave during a long mouse or touch drag',sameGroupDuringDrag&&!canvasGestureActive);canvas.remove(dragGroup);
 const fxSurface=document.createElement('canvas');fxSurface.width=24;fxSurface.height=16;const fxContext=fxSurface.getContext('2d');fxContext.fillStyle='#111111';fxContext.fillRect(0,0,12,16);fxContext.fillStyle='#f5f5f5';fxContext.fillRect(12,0,12,16);const fxAssetId=uid('asset');state.assets[fxAssetId]={id:fxAssetId,dataUrl:fxSurface.toDataURL('image/png'),originalDataUrl:fxSurface.toDataURL('image/png'),mime:'image/png',name:'Effetti test',kind:'upload',source:'test',meta:{}};const fxOriginal=await addImageAsset(fxAssetId,'image');canvas.setActiveObject(fxOriginal);selectionChanged();ok('selected image reports pixels, estimated RAM and print DPI',!$('imageInspectorData').classList.contains('hidden')&&$('imagePixelSize').textContent.includes('24')&&$('imageRamSize').textContent.includes('KB')&&$('imagePrintDpi').textContent.includes('DPI')&&!$('changeResolutionBtn').classList.contains('hidden')&&!$('imageInspectorActions').classList.contains('hidden')&&!$('copyOriginalImageBtn').classList.contains('hidden')&&$('imageCopyActions').classList.contains('hidden'));fxOriginal.set({cropX:2,cropY:3,width:10,height:8});fxOriginal.setCoords();updateSelectedImageInfo();$('imageCopyMenuBtn').click();updateSelectedImageInfo();$('copyCroppedImageBtn').dispatchEvent(new PointerEvent('pointermove',{bubbles:true}));await new Promise(resolve=>setTimeout(resolve,350));const copyMenuOpened=!$('imageCopyActions').classList.contains('hidden')&&$('imageCopyControl').classList.contains('menu-open')&&$('imageCopyMenuBtn').getAttribute('aria-expanded')==='true';$('imageCopyMenuBtn').click();const originalCopy=await createImageBitmap(await nativeImageBlob(fxOriginal,{cropped:false})),croppedCopy=await createImageBitmap(await nativeImageBlob(fxOriginal,{cropped:true}));ok('image inspector copies the original or native-resolution visible crop and keeps its menu open while the pointer enters it',originalCopy.width===24&&originalCopy.height===16&&croppedCopy.width===10&&croppedCopy.height===8&&!$('copyCroppedImageBtn').classList.contains('hidden')&&$('imageCopyMenuBtn').classList.contains('has-options')&&copyMenuOpened);originalCopy.close();croppedCopy.close();fxOriginal.set({cropX:0,cropY:0,width:24,height:16});fxOriginal.setCoords();updateSelectedImageInfo();ok('Crop mode and Reset crop share the row below the image inspector',$('startCropBtn').parentElement===$('resetCropBtn').parentElement&&!!($('imageInspector').compareDocumentPosition($('startCropBtn'))&Node.DOCUMENT_POSITION_FOLLOWING));applyObjectLock(fxOriginal,true);const lockedLeft=fxOriginal.left;window.dispatchEvent(new KeyboardEvent('keydown',{key:'ArrowRight',bubbles:true,cancelable:true}));const lockedData=serializeObject(fxOriginal),lockScratch=new fabric.StaticCanvas(null,{width:100,height:100}),lockedRoundTrip=await instantiateObject(lockedData,lockScratch);ok('fixed position survives project serialization and blocks keyboard movement',fxOriginal.left===lockedLeft&&lockedData.locked&&lockedRoundTrip.chicLocked&&lockedRoundTrip.lockMovementX&&lockedRoundTrip.lockScalingX&&lockedRoundTrip.lockRotation&&!lockedRoundTrip.hasControls);lockScratch.dispose();applyObjectLock(fxOriginal,false);
 const clipBlob=await new Promise(resolve=>fxSurface.toBlob(resolve,'image/png')),clipFile=new File([clipBlob],'appunti.png',{type:'image/png'}),beforeClip=canvas.getObjects().length;await useClipboardImageFile(clipFile,'canvas');ok('clipboard image file inserts a canvas asset',canvas.getObjects().length===beforeClip+1&&canvas.getActiveObject().objectType==='image');await useClipboardImageFile(clipFile,'reference');ok('clipboard image file can become the AI reference',state.aiReference?.name==='appunti.png');const orphanId=uid('asset');state.assets[orphanId]={id:orphanId,dataUrl:'data:image/png;base64,AA==',mime:'image/png',name:'orfano',meta:{}};const removedAssets=pruneUnreachableAssets();ok('unreferenced assets are collected without clearing AI reference or clipboard',removedAssets>0&&!state.assets[orphanId]&&!!state.aiReference&&!!projectClipboard);
 openImageUrl('image');ok('URL dialog keeps Puter explicit and off by default',!$('imageUrlUsePuter').checked&&$('urlDialog').textContent.includes('banda o quota'));
 const savedPuter=window.puter;window.puter={...(savedPuter||{}),ai:savedPuter?.ai||{txt2img(){}},auth:{...(savedPuter?.auth||{}),isSignedIn:()=>false}};syncPuterFetchControls(false);ok('signed-out Puter fetch options are disabled and cleared',!$('imageUrlUsePuter').checked&&$('imageUrlUsePuter').disabled&&!$('clipartUsePuter').checked&&$('clipartUsePuter').disabled&&$('clipartInsertPuterBtn').disabled&&!$('urlDialog').querySelector('[data-puter-login-hint]').classList.contains('hidden'));ok('signed-out automatic translation remains enabled with local fallback and login hint',!$('clipartTranslate').disabled&&!$('emojiTranslate').disabled&&[...document.querySelectorAll('[data-puter-translation-hint]')].every(hint=>!hint.classList.contains('hidden'))&&SEARCH_TRANSLATION_MODEL==='google/gemma-4-31b-it');
 const loginLink=$('urlDialog').querySelector('[data-puter-login-link]');loginLink.click();await new Promise(resolve=>setTimeout(resolve,400));ok('Puter login link closes the URL dialog and opens the AI account section',!$('urlDialog').open&&!$('aiCard').classList.contains('collapsed')&&document.activeElement===$('puterLoginBtn')&&!$('puterLoginDialog').open);window.puter=savedPuter;
 $('graySoftness').value='0';const beforeGray=canvas.getObjects().length;await duplicateProcessedImage('gray');const grayCopy=canvas.getActiveObject(),grayAsset=state.assets[grayCopy.assetId],grayImage=new Image();grayImage.src=grayAsset.dataUrl;await grayImage.decode();const grayCheck=document.createElement('canvas');grayCheck.width=grayImage.naturalWidth;grayCheck.height=grayImage.naturalHeight;grayCheck.getContext('2d').drawImage(grayImage,0,0);const grayPixel=grayCheck.getContext('2d').getImageData(2,2,1,1).data;ok('local black and white creates a non-destructive grayscale copy',canvas.getObjects().length===beforeGray+1&&grayAsset.meta.effect==='bianco e nero'&&grayPixel[0]===grayPixel[1]&&grayPixel[1]===grayPixel[2]&&state.assets[fxAssetId]);
 canvas.setActiveObject(fxOriginal);$('outlineSensitivity').value='70';const beforeOutline=canvas.getObjects().length;await duplicateProcessedImage('outline');const outlineAsset=state.assets[canvas.getActiveObject().assetId],outlineImage=new Image();outlineImage.src=outlineAsset.dataUrl;await outlineImage.decode();const outlineCheck=document.createElement('canvas');outlineCheck.width=outlineImage.naturalWidth;outlineCheck.height=outlineImage.naturalHeight;outlineCheck.getContext('2d').drawImage(outlineImage,0,0);const outlineData=outlineCheck.getContext('2d').getImageData(0,0,outlineCheck.width,outlineCheck.height).data,outlineValues=[];for(let i=0;i<outlineData.length;i+=4)outlineValues.push(outlineData[i]);ok('local outline extracts dark edges on white',canvas.getObjects().length===beforeOutline+1&&outlineAsset.meta.effect==='outline'&&Math.min(...outlineValues)<128&&Math.max(...outlineValues)>240);
 canvas.setActiveObject(fxOriginal);const beforePrepare=canvas.getObjects().length;await prepareColoringAi();ok('coloring AI setup never starts generation',canvas.getObjects().length===beforePrepare&&$('aiProvider').value==='openai-image-generation'&&$('aiModel').value==='gpt-image-2.5-flare'&&$('aiQuality').value==='low'&&$('aiStyle').value==='Pagina da colorare'&&!!state.aiReference&&$('aiPrompt').value.includes('disegno da colorare'));
 ok('image effect controls and labels are available',!!$('duplicateGrayBtn')&&!!$('duplicateOutlineBtn')&&!!$('prepareColoringAiBtn')&&$('graySoftnessLabel').textContent.length>0);const actualMobileMode=mobileBackgroundMode;mobileBackgroundMode=()=>true;syncSettings();ok('mobile background mode forces the small cached model',$('bgModel').disabled&&$('bgModel').value==='isnet_quint8'&&$('bgCacheStatus').textContent.includes('memoria liberata'));mobileBackgroundMode=actualMobileMode;syncSettings();
 const cropSurface=document.createElement('canvas');cropSurface.width=40;cropSurface.height=20;const cropContext=cropSurface.getContext('2d');cropContext.fillStyle='#ebca54';cropContext.fillRect(0,0,40,20);cropContext.fillStyle='#246f9b';cropContext.fillRect(10,4,20,10);const cropObject=new fabric.Image(cropSurface,{left:320,top:310,originX:'center',originY:'center',cropX:10,cropY:4,width:20,height:10,scaleX:.7,scaleY:.7,angle:18});cropObject.objectType='image';cropObject.projectId=uid('obj');canvas.add(cropObject);canvas.setActiveObject(cropObject);const cropScaleX=cropObject.scaleX,cropScaleY=cropObject.scaleY;await resetCrop();ok('reset crop restores the full source without stretching',cropObject.width===40&&cropObject.height===20&&cropObject.cropX===0&&cropObject.cropY===0&&cropObject.scaleX===cropScaleX&&cropObject.scaleY===cropScaleY);
 cropObject.set({cropX:10,cropY:4,width:20,height:10});cropObject.setCoords();canvas.setActiveObject(cropObject);startCrop();const activeCropSession=cropSession;ok('crop mode starts from the current crop and shows the remaining source translucently',!!activeCropSession?.ghost&&!!activeCropSession?.focus&&activeCropSession.rect.width===20&&activeCropSession.rect.height===10&&Math.abs(activeCropSession.rect.width*activeCropSession.rect.scaleX-20*cropScaleX)<.01&&activeCropSession.ghost.width===40&&activeCropSession.ghost.height===20&&activeCropSession.ghost.opacity<.5&&activeCropSession.target.opacity===0&&activeCropSession.focus.visible);activeCropSession.rect.set({width:100,height:80,left:activeCropSession.rect.left+500});activeCropSession.rect.setCoords();activeCropSession.rect.fire('scaling');const boundedCrop=cropBoxFromHelper(cropObject,activeCropSession.rect);ok('crop helper cannot exceed the original image bounds or resize its scale',boundedCrop.width<=40&&boundedCrop.height<=20&&boundedCrop.cropX>=0&&boundedCrop.cropY>=0&&boundedCrop.cropX+boundedCrop.width<=40.01&&boundedCrop.cropY+boundedCrop.height<=20.01&&activeCropSession.rect.width<=40&&activeCropSession.rect.height<=20);$('resetCropActiveBtn').click();await new Promise(resolve=>setTimeout(resolve,30));ok('floating crop controls include a working reset',!cropSession&&cropObject.width===40&&cropObject.height===20&&cropObject.cropX===0&&cropObject.cropY===0&&cropObject.scaleX===cropScaleX&&cropObject.scaleY===cropScaleY&&!!$('resetCropActiveBtn'));canvas.remove(cropObject);
 const edgeCanvas=document.createElement('canvas');edgeCanvas.width=edgeCanvas.height=20;const edgeContext=edgeCanvas.getContext('2d');edgeContext.fillStyle='#19c36a';edgeContext.fillRect(0,0,20,20);edgeContext.fillStyle='#e34848';edgeContext.fillRect(4,4,12,12);const referenceCrop=new fabric.Image(edgeCanvas,{cropX:4,cropY:4,width:12,height:12,scaleX:.4,scaleY:1.7,angle:37}),referenceFile=await canvasCroppedReferenceFile(referenceCrop),referenceImage=new Image(),referenceUrl=URL.createObjectURL(referenceFile);referenceImage.src=referenceUrl;await referenceImage.decode();const referencePixels=document.createElement('canvas');referencePixels.width=referenceImage.naturalWidth;referencePixels.height=referenceImage.naturalHeight;referencePixels.getContext('2d').drawImage(referenceImage,0,0);const referenceData=referencePixels.getContext('2d').getImageData(0,0,referencePixels.width,referencePixels.height).data,opaqueReference=[];for(let i=0;i<referenceData.length;i+=4)if(referenceData[i+3]>240)opaqueReference.push([referenceData[i],referenceData[i+1],referenceData[i+2]]);URL.revokeObjectURL(referenceUrl);ok('canvas AI reference uses only the source crop and ignores layout rotation and scale',referenceImage.naturalWidth===referenceImage.naturalHeight&&opaqueReference.length>0&&opaqueReference.every(pixel=>pixel[0]>180&&pixel[1]<120&&pixel[2]<120));
 const edgeBlob=await new Promise(resolve=>edgeCanvas.toBlob(resolve,'image/png')),edgeColor=await detectEdgeBackgroundColor(edgeBlob);ok('chroma key detects the dominant edge color automatically',edgeColor.toLowerCase()==='#19c36a'&&$('bgEverywhere').checked);ok('every color control has a canvas eyedropper',[...document.querySelectorAll('input[type="color"]')].every(input=>input.nextElementSibling?.classList.contains('color-picker-button')));const rasterFixture={width:1000,height:500,getBoundingClientRect:()=>({left:100,top:50,width:500,height:250})},rasterPoint=canvasRasterPoint({clientX:350,clientY:175},rasterFixture);ok('canvas eyedropper maps screen coordinates to the rendered retina buffer',rasterPoint.x===500&&rasterPoint.y===250);
 const flareProfile=AI_IMAGE_PROFILES.find(profile=>profile.id==='gpt-image-2.5-flare'),sunburstProfile=AI_IMAGE_PROFILES.find(profile=>profile.id==='gpt-image-2.5-sunburst'),grokProfile=AI_IMAGE_PROFILES.find(profile=>profile.id==='grok-imagine-image'),seedreamProfile=AI_IMAGE_PROFILES.find(profile=>profile.id==='byteplus/seedream-5-0-lite-260128'),geminiProfile=AI_IMAGE_PROFILES.find(profile=>profile.id==='google/gemini-3.1-flash-lite-image'),openAiOptions=aiRequestOptions(flareProfile,'low','1:1',{dataUrl:'data:image/png;base64,AA==',mime:'image/png'}),grokOptions=aiRequestOptions(grokProfile,'standard','1:1',null),seedreamOptions=aiRequestOptions(seedreamProfile,'standard','1:1',null),geminiOptions=aiRequestOptions(geminiProfile,'1K','16:9',null),hiddenIds=['rundiffusion/juggernaut-lightning-flux','hidream-ai/hidream-i1-fast','hidream-ai/hidream-i1-dev','qwen/qwen-image'];ok('AI catalog exposes only working curated profiles with canonical Puter IDs and Gemini ratios',AI_IMAGE_PROFILES.length===7&&sunburstProfile.model==='openai/gpt-image-2.5-sunburst'&&sunburstProfile.qualities.join(',')==='low,medium,high'&&$('aiModel').value==='gpt-image-2.5-flare'&&flareProfile.model==='openai/gpt-image-2.5-flare'&&openAiOptions.model==='gpt-image-2.5-flare'&&openAiOptions.provider==='openai-image-generation'&&openAiOptions.ratio.w===1024&&openAiOptions.ratio.h===1024&&openAiOptions.input_images.length===1&&grokOptions.model==='x-ai/grok-imagine-image'&&!('provider' in grokOptions)&&grokOptions.quality==='1k'&&seedreamOptions.model==='byteplus/seedream-5-0-lite-260128'&&!('provider' in seedreamOptions)&&geminiOptions.model==='google/gemini-3.1-flash-lite-image'&&!('provider' in geminiOptions)&&geminiOptions.ratio.w===16&&geminiOptions.ratio.h===9&&geminiOptions.quality==='1K'&&geminiProfile.reference===true&&geminiProfile.ratios.includes('21:9')&&hiddenIds.every(id=>!AI_IMAGE_PROFILES.some(profile=>profile.id===id))&&$('aiReferenceScale').value==='3'&&$('aiCostEstimate').textContent.includes('~'));
 const annotationFixtureCanvas=document.createElement('canvas');annotationFixtureCanvas.width=annotationFixtureCanvas.height=1;const annotationFixture=annotationFixtureCanvas.toDataURL('image/png'),savedReference=state.aiReference,savedAnnotations=state.aiReferenceAnnotations;state.aiReference={dataUrl:annotationFixture,mime:'image/png',name:'annotation.png',width:1,height:1};state.aiReferenceAnnotations={version:1,markers:[{id:'point-1',number:1,type:'point',x:.5,y:.5,w:0,h:0,comment:'Keep user text',color:'#e53935'}]};$('aiProvider').value='openai-image-generation';populateAiModels();$('aiModel').value='gpt-image-2.5-flare';syncAiQualityOptions();syncAiOutputOptions();syncReferenceUI();const annotationPrompt=aiFullPrompt(),annotationRaster=await aiAnnotatedReference(state.aiReference),annotationOptions=aiRequestOptions(aiProfile(),'low','1:1',state.aiReference,annotationRaster),annotationEstimate=aiEstimateBreakdown(),annotationPayload=capturePayload();ok('reference scaling exposes 0.25x through 3x and annotations sit outside its panel',AI_REFERENCE_FACTORS.join(',')==='0.25,0.5,0.75,1,2,3'&&$('aiReferenceScale').value==='3'&&$('aiAnnotationControls').parentElement.id!=='aiReferenceScalePanel');ok('GPT Image 2.5 annotation prompt keeps the point center and brace-delimited user comment',annotationPrompt.includes('ANNOTATION #1')&&annotationPrompt.includes('user_comment: {Keep user text}')&&annotationPrompt.includes('center_normalized')&&!annotationPrompt.includes('bbox_normalized')&&!annotationPrompt.includes('user_comment: "'));ok('GPT Image 2.5 annotation raster is a same-size second input',annotationRaster.width===1&&annotationRaster.height===1&&annotationOptions.input_images.length===2);ok('GPT Image 2.5 annotation state persists and doubles reference estimation',annotationEstimate.referenceCount===2&&annotationPayload.state.aiReferenceAnnotations.markers.length===1);await openAiAnnotationEditor();await new Promise(requestAnimationFrame);ok('annotation modal hosts the shared localized tooltip in the browser top layer',$('aiAnnotationDialog').contains($('desktopTooltip'))&&document.querySelector('[data-ai-annotation-tool="select"]').dataset.tip==='Seleziona marker'&&$('aiAnnotationCommentHandle').dataset.tip==='Apri o chiudi i commenti'&&$('aiAnnotationFit').classList.contains('iconbtn')&&!!$('aiAnnotationFit').querySelector('svg')&&!$('aiAnnotationFit').textContent.trim()&&$('aiAnnotationFit').getAttribute('aria-label')==='Adatta');const annotationSelectTool=document.querySelector('[data-ai-annotation-tool="select"]');annotationSelectTool.dispatchEvent(new PointerEvent('pointerover',{bubbles:true,pointerType:'mouse'}));await new Promise(resolve=>setTimeout(resolve,500));ok('annotation tool hover renders the shared Italian tooltip above the modal',!matchMedia('(hover:hover) and (pointer:fine)').matches||($('aiAnnotationDialog').contains($('desktopTooltip'))&&!$('desktopTooltip').classList.contains('hidden')&&$('desktopTooltip').textContent==='Seleziona marker'));annotationSelectTool.dispatchEvent(new PointerEvent('pointerout',{bubbles:true,pointerType:'mouse'}));const annotationWorkspaceBefore=$('aiAnnotationWorkspace').getBoundingClientRect(),annotationPanelStyle=getComputedStyle($('aiAnnotationCommentPanel')),annotationToolbar=$('aiAnnotationDialog').querySelector('.ai-annotation-toolbar'),annotationToolbarStyle=getComputedStyle(annotationToolbar),annotationToolbarRect=annotationToolbar.getBoundingClientRect(),annotationStageWhite=getComputedStyle($('aiAnnotationStage')).backgroundColor,annotationHandle=$('aiAnnotationCommentHandle'),annotationHandleRect=annotationHandle.getBoundingClientRect(),annotationHandleHit=document.elementFromPoint(annotationHandleRect.left+annotationHandleRect.width/2,annotationHandleRect.top+annotationHandleRect.height/2);ok('closed annotation panel keeps its comment handle visible and interactive',annotationHandleRect.top<annotationWorkspaceBefore.bottom-5&&(annotationHandleHit===annotationHandle||annotationHandle.contains(annotationHandleHit)));$('aiAnnotationPreviewBackground').click();const annotationStageDark=getComputedStyle($('aiAnnotationStage')).backgroundColor,annotationRasterAfterPreviewToggle=await aiAnnotatedReference(state.aiReference);ok('annotation transparency preview defaults white, toggles dark and never changes the submitted raster',annotationStageWhite==='rgb(255, 255, 255)'&&annotationStageDark==='rgb(17, 17, 17)'&&annotationRasterAfterPreviewToggle.dataUrl===annotationRaster.dataUrl);openAiAnnotationComments(true);await new Promise(resolve=>setTimeout(resolve,320));const annotationWorkspaceAfter=$('aiAnnotationWorkspace').getBoundingClientRect();ok('annotation comments slide as a fixed overlay without resizing the canvas workspace',annotationPanelStyle.position==='absolute'&&Math.abs(annotationWorkspaceAfter.height-annotationWorkspaceBefore.height)<.5&&$('aiAnnotationCommentPanel').classList.contains('open'));ok('mobile annotation toolbar keeps its scrollbar below the controls',!matchMedia('(max-width:720px)').matches||(annotationToolbarRect.height>=50&&parseFloat(annotationToolbarStyle.paddingBottom)>=9));closeAiAnnotationEditor();ok('closing annotation modal restores the shared tooltip outside the dialog',!$('aiAnnotationDialog').contains($('desktopTooltip'))&&$('desktopTooltip').classList.contains('hidden'));const opaqueReferenceCanvas=document.createElement('canvas');opaqueReferenceCanvas.width=opaqueReferenceCanvas.height=2;const opaqueReferenceContext=opaqueReferenceCanvas.getContext('2d');opaqueReferenceContext.fillStyle='#fff';opaqueReferenceContext.fillRect(0,0,2,2);state.aiReference={dataUrl:opaqueReferenceCanvas.toDataURL('image/png'),mime:'image/png',name:'opaque.png',width:2,height:2};await openAiAnnotationEditor();ok('opaque references disable the transparency background control with an explanatory Italian tooltip',$('aiAnnotationPreviewBackground').disabled&&!$('aiAnnotationStage').classList.contains('preview-dark')&&$('aiAnnotationPreviewBackground').dataset.tip==='L’immagine non contiene trasparenze');closeAiAnnotationEditor();$('aiModel').value='gpt-image-2';syncAiQualityOptions();syncAiOutputOptions();syncReferenceUI();ok('incompatible models disable annotation editing without deleting saved marker data',$('openAiAnnotation').disabled&&state.aiReferenceAnnotations.markers.length===1);state.aiReference=savedReference;state.aiReferenceAnnotations=savedAnnotations;$('aiModel').value='gpt-image-2.5-flare';syncAiQualityOptions();syncAiOutputOptions();syncReferenceUI();
 const generatedFixture='data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVQIHWP4z8DwHwAFgAI/ScL9WQAAAABJRU5ErkJggg==';aiSetPendingPreview({result:generatedFixture,profile:geminiProfile},new Error('fetch blocked'));ok('completed AI output remains recoverable when automatic asset ingestion fails',aiPendingGenerated?.result===generatedFixture&&!$('aiGeneratedFallback').classList.contains('hidden')&&$('aiGeneratedFallbackImage').src.startsWith('data:image/png')&&!!$('aiGeneratedDialogRetry')&&!!$('aiGeneratedDialogDownload'));aiClearPendingPreview();ok('AI recovery preview clears after successful recovery',aiPendingGenerated===null&&$('aiGeneratedFallback').classList.contains('hidden'));
 const minSquare=aiOpenAiDimensions('1:1','min'),minA4=aiOpenAiDimensions('210:297','min'),presetA4=aiOpenAiDimensions('210:297','2048'),invalidOpenAi=aiOpenAiDimensions('1:4','1024'),invalidCustom=aiOpenAiCustomDimensions(1024,1000),squareMin=aiOpenAiOutputEstimate(flareProfile,'low',{width:816,height:816}),a4Min=aiOpenAiOutputEstimate(flareProfile,'low',{width:688,height:976}),unknownA4=aiOpenAiOutputEstimate(flareProfile,'low',{width:1024,height:1456});$('aiCustomResolution').checked=false;$('aiResolution').value='1024';$('aiRatio').value='1:1';validateAiOutputSelection(false);$('aiRatio').add(new Option('1:4','1:4'));$('aiRatio').value='1:4';const invalidRestored=!validateAiOutputSelection(false)&&$('aiRatio').value==='1:1';$('aiCustomResolution').checked=true;$('aiCustomWidth').value='1456';$('aiCustomHeight').value='2048';const exactCustom=validateAiOutputSelection(false)&&aiResolutionDimensions(flareProfile).width===1456&&aiResolutionDimensions(flareProfile).height===2048&&$('aiResolution').disabled&&$('aiRatio').disabled;ok('OpenAI output validation and estimate use the official non-linear calculator formula',minSquare.width===816&&minSquare.height===816&&minA4.width===688&&minA4.height===976&&presetA4.width===2048&&presetA4.height===2912&&invalidOpenAi.valid===false&&invalidCustom.valid===false&&invalidRestored&&exactCustom&&squareMin.tokens===171&&a4Min.tokens===118&&unknownA4.tokens===154&&aiOpenAiOutputEstimate(flareProfile,'medium',{width:2048,height:2912}).tokens===813&&aiOpenAiReferenceProxyTokens(1024,1456)===1767&&Math.abs(aiEquivalentCredits(.00588)-11.76)<.001);
 populateAiModels();$('aiProvider').value='xai';populateAiModels();$('aiResolution').value='2k';const grokDimensions=aiResolutionDimensions(grokProfile,'1:1'),estimate=aiEstimateBreakdown();ok('AI separates provider resolution from quality and estimates dashboard Credits including references',grokDimensions.width===2048&&grokDimensions.height===2048&&estimate.usd>estimate.outputUsd&&$('aiCostEstimate').textContent.includes('crediti')&&!$('aiCostEstimate').textContent.includes('microcrediti'));$('aiProvider').value='openai-image-generation';populateAiModels();
 ok('Prompt Library provides persistent CRUD, tag chips, combined filters, clipboard, style, optional reference, merge choices and sharing controls',typeof promptLibraryTx==='function'&&!!$('openPromptLibraryBtn')&&!!$('saveCurrentPromptBtn')&&!!$('promptLibrarySearch')&&!!$('promptLibraryTagFilter')&&!!$('promptLibraryTagChoices')&&!!$('promptEntryTagInput')&&!!$('promptEntryIncludeReference')&&!!$('copyPromptEntryBtn')&&!!$('copyPromptImageBtn')&&!!$('pasteAiPromptBtn')&&!!$('exportPromptLibraryBtn')&&!!$('sharePromptLibraryBtn')&&!!$('importPromptLibraryBtn')&&!!$('promptLibraryImportDialog')&&!!$('showRawAiPrompt')&&!!$('rawAiPromptDialog'));const promptProbe={id:'v7-prompt-probe',title:'Scheda leone',tags:['animali','scuola'],description:'Test didattico',prompt:'Disegna un leone',style:'Pagina da colorare',reference:null,createdAt:new Date().toISOString(),updatedAt:new Date().toISOString()};await promptLibraryTx('readwrite',store=>store.put(promptProbe));await loadPromptLibrary();openPromptEditor(promptProbe);ok('existing Prompt Library entries prioritize the green use action over update',$('usePromptEntryBtn').classList.contains('accent')&&!$('savePromptEntryBtn').classList.contains('accent')&&$('usePromptEntryBtn').compareDocumentPosition($('savePromptEntryBtn'))&Node.DOCUMENT_POSITION_FOLLOWING);$('promptLibraryBackBtn').click();$('promptLibrarySearch').value='animali';renderPromptLibrary();promptLibrarySelectedTags=['scuola'];renderPromptLibrary();ok('Prompt Library writes, searches tags, combines tag filters and reloads entries from IndexedDB',promptLibraryEntries.some(entry=>entry.id===promptProbe.id)&&$('promptLibraryItems').querySelector('[data-prompt-id="v7-prompt-probe"]')&&$('promptLibraryTagChoices').querySelector('[data-prompt-tag="scuola"].active'));$('promptLibrarySearch').value='';promptLibrarySelectedTags=[];await applyPromptLibraryImport({type:'chicCanva-prompt-library',version:1,prompts:[{title:'Scheda leone',prompt:'Leone aggiornato'}]},{mode:'append',replaceSameName:true});await loadPromptLibrary();const replacedPrompt=promptLibraryEntries.find(entry=>entry.id===promptProbe.id);await applyPromptLibraryImport({type:'chicCanva-prompt-library',version:1,prompts:[{title:'Scheda leone',prompt:'Leone duplicato'}]},{mode:'append',replaceSameName:false});await loadPromptLibrary();ok('Prompt Library append can either replace a same-name entry or preserve it as a new prompt',replacedPrompt?.prompt==='Leone aggiornato'&&promptLibraryEntries.filter(entry=>entry.title==='Scheda leone').length===2);for(const entry of promptLibraryEntries.filter(entry=>entry.title==='Scheda leone'))await promptLibraryTx('readwrite',store=>store.delete(entry.id));
 ok('file sharing exposes generated-file, project JSON and P2P entry points only in the export section',!!$('shareGeneratedFileBtn')&&!!$('shareProjectJsonBtn')&&!!$('openP2pTransferBtn')&&!$('exportJsonBtn').parentElement.querySelector('.share-file-button')&&typeof buildUnifiedExportArtifact==='function'&&typeof projectExportArtifact==='function');ok('export and Prompt Library transfer rows keep export wide, import compact and a visual separator',document.querySelector('.export-json-actions>.transfer-separator')&&$('importJsonPanel').classList.contains('import-compact')&&document.querySelector('.prompt-library-transfer-actions>.transfer-separator')&&$('importPromptLibraryBtn').classList.contains('import-compact'));const shareProbeNavigation={share(){},canShare:({files})=>files?.[0]?.type==='application/pdf'};ok('native share visibility is based on the exact file capability rather than operating-system detection',canNativeShareFile('probe.pdf','application/pdf',shareProbeNavigation,true)&&!canNativeShareFile('probe.json','application/json',shareProbeNavigation,true)&&!canNativeShareFile('probe.pdf','application/pdf',shareProbeNavigation,false));const generatedCode=createP2pCode(),compactCode=normalizeP2pCode(generatedCode),wrongCode=compactCode.slice(0,11)+(compactCode.at(-1)==='Z'?'Y':'Z');ok('P2P one-time codes include a checksum and survive display normalization',/^([2-9A-HJ-NP-Z]{4}-){2}[2-9A-HJ-NP-Z]{4}$/.test(generatedCode)&&compactCode.length===12&&validateP2pCode(generatedCode)&&!validateP2pCode(wrongCode));const transferBytes=new TextEncoder().encode('{}'),transferManifest=p2pManifestFor([{id:'probe',type:'project',name:'Probe',fileName:'Probe.json',bytes:transferBytes,sha256:await sha256Hex(transferBytes)}]);ok('P2P manifests preserve selection metadata, byte size and SHA-256 validation',validateP2pManifest(transferManifest)===transferManifest&&transferManifest.items[0].size===2&&transferManifest.items[0].sha256.length===64&&typeof ChicTransferVendor?.joinRoom==='function'&&typeof ChicTransferVendor?.jsQR==='function');const qrProbe=qrSvg(generatedCode);openP2pDialog();ok('P2P vendor, QR rendering and dialog initialize without starting a network session',p2pAvailable()&&qrProbe.startsWith('<svg')&&document.getElementById('p2pTransferDialog').open&&document.getElementById('p2pStartView').hidden===false&&!p2pRoom);document.getElementById('p2pTransferDialog').close();
 const relayCredentials=await p2pRoomCredentials(generatedCode),relayCredentialsAgain=await p2pRoomCredentials(generatedCode);ok('P2P peers derive the same maintained relay subset from the one-time code',relayCredentials.relayUrls.length===5&&relayCredentials.relayUrls.join('|')===relayCredentialsAgain.relayUrls.join('|')&&relayCredentials.relayUrls.every(url=>P2P_RELAY_POOL.includes(url))&&!relayCredentials.relayUrls.some(url=>/halifax|aaroniumii/.test(url)));const savedCustomRelay=localStorage.getItem(P2P_CUSTOM_RELAY_KEY);localStorage.setItem(P2P_CUSTOM_RELAY_KEY,'wss://relay.example.test');const customRelayConfig=p2pJoinConfig(relayCredentials);if(savedCustomRelay===null)localStorage.removeItem(P2P_CUSTOM_RELAY_KEY);else localStorage.setItem(P2P_CUSTOM_RELAY_KEY,savedCustomRelay);let invalidRelayRejected=false;try{normalizeP2pRelayUrl('https://relay.example.test')}catch(error){invalidRelayRejected=true}ok('custom relay is validated and added without replacing the common fallback',customRelayConfig.relayConfig.urls[0]==='wss://relay.example.test'&&relayCredentials.relayUrls.every(url=>customRelayConfig.relayConfig.urls.includes(url))&&invalidRelayRejected&&!!$('p2pSignalingError')&&!!$('p2pOpenRelaySettings'));const fakeActions=new Map(),sentAction={};const fakeRoom={makeAction(name){const action={onMessage:null,onReceiveProgress:null,send:async(data,options)=>{sentAction.name=name;sentAction.data=data;sentAction.options=options}};fakeActions.set(name,action);return action}},adaptedActions=p2pActions(fakeRoom);let adaptedMessage=null;adaptedActions.getManifest((data,peerId,metadata)=>adaptedMessage={data,peerId,metadata});fakeActions.get('manifest').onMessage({probe:true},{peerId:'peer-a',metadata:{kind:'test'}});await adaptedActions.sendManifest({hello:true},'peer-b');ok('Trystero 0.25 object actions preserve targeted send and receive metadata',adaptedMessage?.peerId==='peer-a'&&adaptedMessage?.metadata?.kind==='test'&&sentAction.name==='manifest'&&sentAction.options.target==='peer-b');
 ok('Cloud Storage is a bilingual collapsible section with secure-session, explorer, backup and sync controls',!!$('cloudStorageCard')&&!!$('cloudExplorerDialog')&&!!$('cloudRememberSession')&&!!$('cloudAutomaticBackup')&&!!$('cloudAutomaticSync')&&!!$('resumeCloudMemory')&&[...$('sidebar').querySelectorAll('.quick-jump button')].some(button=>button.dataset.jumpSource==='Cloud')&&CLOUD_ROOT_NAME==='chicCanva Cloud'&&CLOUD_BACKUP_NAME==='.backups'&&CLOUD_SYNC_NAME==='.synch');
 ok('AI offers background-removal and chroma-key options',!!$('aiRemoveBackground')&&!$('aiRemoveBackground').checked&&!!$('aiChromaKey')&&!$('aiChromaKey').checked&&AI_CHROMA_INSTRUCTION.includes('uniforme'));
 canvas.setActiveObject(fxOriginal);const physicalWidthBefore=fxOriginal.width*fxOriginal.scaleX,physicalHeightBefore=fxOriginal.height*fxOriginal.scaleY;resolutionTarget=fxOriginal;$('resolutionScale').value='7';$('resolutionPngQuality').value='100';await resizeSelectedImage();const resizedPixels=imageElementSize(fxOriginal);ok('quality replacement resamples pixels while preserving page geometry and undoable source',resizedPixels.width===48&&resizedPixels.height===32&&Math.abs(fxOriginal.width*fxOriginal.scaleX-physicalWidthBefore)<.01&&Math.abs(fxOriginal.height*fxOriginal.scaleY-physicalHeightBefore)<.01&&state.assets[fxOriginal.assetId].meta.resolutionFactor===2&&state.assets[fxOriginal.assetId].meta.pngQuality===100&&state.assets[fxAssetId]);
 ok('PDF import UI offers scope, destination, orientation and quality',!!$('importPdfBtn')&&!!$('pdfFile')&&!!$('pdfImportDialog')&&document.querySelectorAll('input[name="pdfImportScope"]').length===2&&document.querySelectorAll('input[name="pdfImportDestination"]').length===2&&['auto','portrait','landscape'].every(value=>$('pdfImportOrientation').querySelector(`option[value="${value}"]`)));
 const samplePdf=new jspdf.jsPDF({unit:'pt',format:[100,200],orientation:'portrait'});samplePdf.text('UNO',10,30);samplePdf.addPage([200,100],'landscape');samplePdf.text('DUE',10,30);const projectsBeforePdf=projects.length;await preparePdfImport(new File([samplePdf.output('arraybuffer')],'schede-test.pdf',{type:'application/pdf'}));ok('embedded PDF.js reads the selected PDF locally',pdfImportDocument?.numPages===2&&$('pdfImportDialog').open&&$('pdfImportInfo').textContent.includes('2 pagine'));$('pdfImportQuality').value='1';$('pdfImportOrientation').value='auto';document.querySelector('input[name="pdfImportDestination"][value="new"]').checked=true;await confirmPdfImport();ok('PDF pages become fitted PNG images in a new A4 project',projects.length===projectsBeforePdf+1&&state.pages.custom.length===2&&state.pages.custom[0].widthMm===210&&state.pages.custom[0].heightMm===297&&state.pages.custom[1].widthMm===297&&state.pages.custom[1].heightMm===210&&state.pages.custom.every(page=>page.objects.length===1&&page.objects[0].objectType==='image')&&Object.values(state.assets).every(asset=>asset.mime==='image/png'&&asset.meta.pdfJsVersion===PDFJS_VERSION));
 const editProbe=new fabric.Text('CIAO',{left:90,top:110,fontSize:48,fill:'transparent',stroke:'#000000',strokeWidth:2,originX:'left',originY:'top'});editProbe.objectType='text';editProbe.projectId=uid('obj');canvas.add(editProbe);canvas.setActiveObject(editProbe);refreshTextEditTrigger();const editTopLeft=editProbe.getPointByOrigin('left','top'),editWidth=editProbe.getBoundingRect(true,true).width;$('editSelectedTextBtn').click();canvas.discardActiveObject();canvas.fire('selection:cleared');await new Promise(resolve=>setTimeout(resolve,0));const editSurvivesSidebarFocus=textEditSession?.object===editProbe&&!$('textEditOptions').classList.contains('hidden')&&$('buildTextBtn').textContent==='Aggiorna testo'&&$('rebuildTextBtn').hidden&&$('fitTextBtn').hidden&&!$('smartEmoji').disabled&&!$('uppercaseText').disabled;$('textInput').value='BENVENUTI';$('textEditConstraint').value='width';$('textEditKeepAspect').checked=true;await updateEditedText();const editedText=canvas.getActiveObject(),editedTopLeft=editedText.getPointByOrigin('left','top');ok('a selected text can be edited in place after sidebar focus, with fixed top-left anchor and width constraint',editSurvivesSidebarFocus&&editedText.text==='BENVENUTI'&&Math.hypot(editedTopLeft.x-editTopLeft.x,editedTopLeft.y-editTopLeft.y)<.01&&Math.abs(editedText.getBoundingRect(true,true).width-editWidth)<.1&&$('buildTextBtn').textContent==='Aggiungi testo'&&$('textEditOptions').classList.contains('hidden')&&!$('rebuildTextBtn').hidden);canvas.remove(editedText);
 const historyProbe=new fabric.Text('STORICO',{left:150,top:120,fontSize:42,fill:'transparent',stroke:'#000000',strokeWidth:2});historyProbe.objectType='text';historyProbe.projectId=uid('obj');canvas.add(historyProbe);saveActivePage();commitHistory();const historyBaseline=historyEntries[historyCursor],historyBaselineSignature=historySignature(historyBaseline),savedPromptDraft=$('aiPrompt').value,savedDefaultStroke=state.strokeColor;$('aiPrompt').value='bozza che non modifica il foglio';state.strokeColor='#123456';$('strokeColor').value='#123456';commitHistory();const settingsIgnored=historyEntries[historyCursor]===historyBaseline&&historySignature(historyEntries[historyCursor])===historyBaselineSignature;historyProbe.set({stroke:'#e34848'});historyProbe.setCoords();saveActivePage();commitHistory();const objectRecorded=historyEntries[historyCursor]!==historyBaseline&&historySignature(historyEntries[historyCursor])!==historyBaselineSignature,compactKeys=Object.keys(historyEntries[historyCursor].state).sort().join(',');ok('undo history ignores drafts and tool defaults but records styles applied to canvas objects',settingsIgnored&&objectRecorded&&compactKeys==='customCounter,mode,pageIndex,workspaceCustom');$('aiPrompt').value=savedPromptDraft;state.strokeColor=savedDefaultStroke;$('strokeColor').value=savedDefaultStroke;canvas.remove(historyProbe);saveActivePage();commitHistory();
  const savedGuideState=cloneData(state.guideSnap),savedClassicSnap=state.snap.enabled,guideProbe=new fabric.Rect({left:canvas.width-97,top:100,width:100,height:60,originX:'left',originY:'top'});guideProbe.projectId=uid('obj');guideProbe.objectType='shape';canvas.add(guideProbe);state.snap.enabled=false;state.guideSnap={enabled:true,page:false,edges:true,objects:false,spacing:false};applyAlignmentGuides({target:guideProbe,e:{}});const pageEdgeBounds=guideProbe.getBoundingRect(true,true);clearAlignmentGuides();const fitProbe=new fabric.Rect({left:80,top:70,width:210,height:297,strokeWidth:0,originX:'left',originY:'top'});fitProbe.projectId=uid('obj');fitProbe.objectType='shape';canvas.add(fitProbe);$('fitAxis').value='width';$('fitMarginMm').value='10';canvas.setActiveObject(fitProbe);fitSelectionFromContext(true);const marginBounds=fitProbe.getBoundingRect(true,true),marginPx=10*PX_PER_MM,marginTargetWidth=canvas.width-2*marginPx,marginTargetHeight=canvas.height-2*marginPx,marginFits=marginBounds.left>=marginPx-.2&&marginBounds.top>=marginPx-.2&&marginBounds.left+marginBounds.width<=canvas.width-marginPx+.2&&marginBounds.top+marginBounds.height<=canvas.height-marginPx+.2&&(Math.abs(marginBounds.width-marginTargetWidth)<.2||Math.abs(marginBounds.height-marginTargetHeight)<.2);canvas.setActiveObject(fitProbe);fitSelectionFromContext(false);const fullBounds=fitProbe.getBoundingRect(true,true);ok('page-edge guides and both contextual fit modes use the configured page geometry',Math.abs(pageEdgeBounds.left+pageEdgeBounds.width-canvas.width)<.1&&marginFits&&Math.abs(fullBounds.left)<.2&&Math.abs(fullBounds.top)<.2&&Math.abs(fullBounds.width-canvas.width)<.2&&Math.abs(fullBounds.height-canvas.height)<.2&&!!$('fitSelectionMenu')&&$('fitMarginMm').closest('.fit-margin-setting').previousElementSibling?.querySelector('#snapRotationStep')!==null&&!!($('pageMoveLeft').compareDocumentPosition($('customPageName').previousElementSibling)&Node.DOCUMENT_POSITION_FOLLOWING));canvas.remove(guideProbe,fitProbe);state.guideSnap=savedGuideState;state.snap.enabled=savedClassicSnap;syncAlignmentUi();
 const mobileGuideToc=$('helpLayer').querySelector('.guide-toc'),mobileGuideStyle=getComputedStyle(mobileGuideToc),helpDialog=$('helpLayer').querySelector('.help-dialog'),helpDialogWidth=parseFloat(getComputedStyle(helpDialog).width),italianGuideTopics=[...$('helpLayer').querySelectorAll('.guide-topic')],italianGuideTopicIds=italianGuideTopics.map(section=>section.id);mobileGuideToc.open=true;mobileGuideToc.querySelector('a').dispatchEvent(new MouseEvent('click',{bubbles:true,cancelable:true}));await new Promise(requestAnimationFrame);const mobileGuideCollapsedStyle=getComputedStyle(mobileGuideToc),contextGuide=$('helpLayer').querySelector('#context-menus-guide'),allTopicsIllustrated=italianGuideTopics.every(section=>section.querySelector('.guide-real-ui img[src^="data:image/png;base64,"]'));ok('guide is extensive, every feature is illustrated, context menus have their own section, and the index stays inside the dialog',$('helpLayer').textContent.length>60000&&italianGuideTopicIds.length>=25&&$('helpLayer').querySelectorAll('.guide-real-ui img[src^="data:image/png;base64,"]').length>=36&&allTopicsIllustrated&&!!contextGuide&&!!contextGuide.querySelector('.guide-context-table')&&!$('helpLayer').querySelector('#icons .guide-context-table')&&$('helpLayer').querySelectorAll('.guide-options').length>=20&&$('helpLayer').querySelectorAll('.guide-case').length>=20&&$('helpLayer').querySelectorAll('.guide-ui-sample').length===0&&$('helpLayer').querySelectorAll('.guide-flow').length>=8&&$('helpLayer').querySelectorAll('.guide-icon-card').length>=30&&$('helpLayer').querySelectorAll('.guide-toolbar-table tbody tr').length>=21&&$('helpLayer').querySelectorAll('.guide-context-table tbody tr').length>=5&&(innerWidth<=720||helpDialogWidth>=Math.min(1200,innerWidth-50))&&(innerWidth<=900||parseFloat(mobileGuideStyle.maxHeight)<=innerHeight*.9-100)&&!$('helpLayer').textContent.includes('IndexedDB')&&!$('helpLayer').textContent.includes('providerModel')&&(innerWidth>720||(mobileGuideCollapsedStyle.position==='sticky'&&!mobileGuideToc.open&&parseFloat(mobileGuideCollapsedStyle.maxHeight)>0&&(innerWidth>720||(parseFloat(mobileGuideCollapsedStyle.top)>=56&&parseFloat(mobileGuideCollapsedStyle.maxHeight)<=42&&parseFloat(mobileGuideCollapsedStyle.paddingTop)<=7)))));
 const italianGuideLength=$('helpLayer').textContent.length,languageSetDimensions=canvas.setDimensions,languageCanvasDimensionCalls=[];canvas.setDimensions=function(dimensions,options){languageCanvasDimensionCalls.push({dimensions,options});return languageSetDimensions.call(this,dimensions,options)};applyLanguage('en');const dynamicTranslationProbe=document.createElement('div');dynamicTranslationProbe.textContent='Riprendi progetto';document.body.append(dynamicTranslationProbe);await new Promise(resolve=>setTimeout(resolve,30));canvas.setDimensions=languageSetDimensions;ok('language changes keep the overflowing toolbar row and settled canvas raster stable',languageCanvasDimensionCalls.every(call=>call.options?.cssOnly)&&document.querySelector('.workspace>.toolbar').clientHeight>0);ok('English localization updates controls, tooltips, dynamic dialogs and persisted preference',document.documentElement.lang==='en'&&localStorage.getItem('chiccanva.language.v1')==='en'&&$('helpBtn').textContent.includes('Guide')&&$('newProjectBtn').textContent.includes('Project')&&$('copyTool').getAttribute('data-tip').includes('Copy objects')&&dynamicTranslationProbe.textContent==='Resume project'&&$('languagePickerDesktop').querySelector('[data-language-flag]').classList.contains('flag-gb')&&$('languagePickerMobile').querySelector('[data-language-name]').textContent==='English'&&$('cloudStorageCard').querySelector('.card-sub').textContent==='Open, save, and synchronize projects with MEGA.'&&$('cloudLoginBtn').textContent==='Sign in to MEGA'&&$('cloudBackBtn').dataset.tip==='Back'&&$('cloudUnavailable').textContent==='Cloud Storage is available only when chicCanva is hosted over HTTPS.');dynamicTranslationProbe.remove();ok('new project, page and duplicate names follow the active UI language',localizedProjectName(2)==='Project 2'&&localizedPageName(3)==='Page 3'&&localizedUntitledPageName()==='Untitled page'&&localizedCopySuffix()===' copy');ok('English search does not expose the Italian-to-English translation controls',$('emojiTranslate').disabled&&!$('emojiTranslate').checked&&$('emojiTranslate').closest('label').classList.contains('hidden')&&$('clipartTranslate').disabled&&!$('clipartTranslate').checked&&$('clipartTranslate').closest('label').classList.contains('hidden'));const englishGuideTopicIds=[...$('helpLayer').querySelectorAll('.guide-topic')].map(section=>section.id),missingEnglishTargets=[...$('helpLayer').querySelectorAll('a[href^="#"]')].filter(link=>!$('helpLayer').querySelector(link.getAttribute('href')));ok('English guide is a complete equivalent localized manual',$('helpLayer').textContent.length>45000&&$('helpLayer').textContent.includes('Which model to choose')&&$('helpLayer').textContent.includes('Guided example')&&$('helpLayer').textContent.includes('Useful words in plain language')&&!$('helpLayer').textContent.includes('Guida completa')&&JSON.stringify(englishGuideTopicIds)===JSON.stringify(italianGuideTopicIds)&&missingEnglishTargets.length===0&&italianGuideLength>60000);$('aiPrompt').value='A red school book';$('aiStyle').value='Pagina da colorare';ok('English AI composition uses the English style preset and preserves the user prompt language',aiFullPrompt().includes('Simple coloring book page')&&!aiFullPrompt().includes('language of the user-written prompt'));
 const englishJumps=[...$('sidebar').querySelectorAll('.quick-jump button')].map(button=>button.textContent.trim()),englishEmoji=[...$('emojiCategory').options].map(option=>option.textContent);openPromptEditor({id:'i18n-probe',title:'User title',prompt:'User prompt',tags:[],style:'prompt'});ok('dynamic English jump labels are localized',englishJumps.includes('Page')&&englishJumps.includes('Shapes')&&englishJumps.includes('Export'));ok('dynamic English toolbar tips are localized',$('pasteTool').dataset.tip==='Paste objects · Ctrl+V');ok('dynamic English annotation tooltips are localized',document.querySelector('[data-ai-annotation-tool="select"]').dataset.tip==='Select marker'&&$('deleteSelectedAiAnnotation').dataset.tip.startsWith('Delete selected annotation')&&$('aiAnnotationPreviewBackground').dataset.tip==='The image has no transparency'&&$('aiAnnotationCommentHandle').dataset.tip==='Open or close comments'&&$('aiAnnotationFit').dataset.tip==='Fit'&&$('aiAnnotationFit').getAttribute('aria-label')==='Fit');ok('dynamic English emoji labels preserve stable values',englishEmoji.includes('Most relevant')&&englishEmoji.includes('Smileys and emotions')&&$('emojiCategory').querySelector('option[value="smileys-emotion"]'));ok('dynamic English Prompt Library actions are localized',$('savePromptEntryBtn').textContent==='Update prompt');ok('dynamic English help title is localized',$('helpTitle').textContent==="Hi! I'm Chicca 👋");showPromptLibraryList();updateAiEstimate();ok('AI estimate keeps separate main and USD lines ready for mobile layout',!!$('aiCostEstimateValue').querySelector('.ai-estimate-mainline')&&!!$('aiCostEstimateValue').querySelector('.ai-estimate-usd')&&!$('aiCostEstimateValue').textContent.includes('Stima Listino'));
 openP2pDialog();const englishRelayLink=$('p2pOpenRelaySettings').textContent;$('p2pOpenRelaySettings').click();const englishRelaySettings=$('p2pRelaySettingsView').textContent;ok('sharing, P2P and relay settings localize visible controls in English',$('p2pTransferDialog').querySelector('h2').textContent==='P2P Project Sharing'&&$('p2pChooseSend').textContent.includes('Send projects')&&$('p2pChooseReceive').textContent.includes('Receive projects')&&englishRelayLink==='Configure a custom relay'&&englishRelaySettings.includes('Custom signaling relay')&&englishRelaySettings.includes('Save relay')&&t('In attesa del destinatario…')==='Waiting for the recipient…'&&t('QR letto. Premi Connetti per continuare.').includes('Press Connect'));$('p2pTransferDialog').close();
 applyLanguage('it');await new Promise(resolve=>setTimeout(resolve,30));ok('switching back restores Italian text, translation preference and full guide',document.documentElement.lang==='it'&&$('helpBtn').textContent.includes('Guida')&&$('newProjectBtn').textContent.includes('Progetto')&&$('helpLayer').textContent.includes('Manuale illustrato di chicCanva')&&$('helpLayer').textContent.includes('Esempio guidato')&&$('languagePickerDesktop').querySelector('[data-language-flag]').classList.contains('flag-it')&&$('cloudStorageCard').querySelector('.card-sub').textContent==='Apri, salva e sincronizza progetti con MEGA.'&&$('cloudLoginBtn').textContent==='Accedi a MEGA'&&$('cloudBackBtn').dataset.tip==='Indietro'&&$('cloudUnavailable').textContent==='Cloud Storage è disponibile solo quando chicCanva è pubblicata su HTTPS.'&&!$('emojiTranslate').disabled&&$('emojiTranslate').checked&&!$('clipartTranslate').disabled&&$('clipartTranslate').checked);ok('Italian AI composition uses the localized style preset',aiFullPrompt().includes('Semplice pagina da colorare')&&!aiFullPrompt().includes('lingua del prompt scritto dall’utente'));
 const italianJumps=[...$('sidebar').querySelectorAll('.quick-jump button')].map(button=>button.textContent.trim()),italianEmoji=[...$('emojiCategory').options].map(option=>option.textContent);openPromptEditor({id:'i18n-probe',title:'User title',prompt:'User prompt',tags:[],style:'prompt'});ok('switching back restores dynamic Italian labels without changing internal keys or user content',italianJumps.includes('Pagina')&&italianJumps.includes('Forme')&&italianJumps.includes('Esporta')&&$('pasteTool').dataset.tip==='Incolla oggetti · Ctrl+V'&&italianEmoji.includes('Più rilevanti')&&italianEmoji.includes('Faccine ed emozioni')&&document.querySelector('[data-ai-annotation-tool="select"]').dataset.tip==='Seleziona marker'&&$('aiAnnotationPreviewBackground').dataset.tip==='L’immagine non contiene trasparenze'&&$('aiAnnotationCommentHandle').dataset.tip==='Apri o chiudi i commenti'&&$('aiAnnotationFit').dataset.tip==='Adatta'&&$('aiAnnotationFit').getAttribute('aria-label')==='Adatta'&&$('emojiCategory').querySelector('option[value="smileys-emotion"]')?.value==='smileys-emotion'&&$('savePromptEntryBtn').textContent==='Aggiorna prompt'&&$('promptEntryTitle').value==='User title'&&$('helpTitle').textContent==='Ciao! Sono Chicca 👋'&&$('p2pOpenRelaySettings').textContent==='Configura un relay personalizzato');showPromptLibraryList();
 rememberSearchTranslation('prova memoria','memory test');localStorage.setItem(PUTER_IMAGE_PRICING_CACHE_KEY,JSON.stringify({fetchedAt:Date.now(),db:PUTER_IMAGE_PRICING_FALLBACK}));localStorage.setItem(PUTER_IMAGE_ACTUAL_CACHE_KEY,JSON.stringify({probe:{credits:12,usd:.006,at:Date.now()}}));ok('translation cache contains successful learned terms',cachedSearchTranslation('prova memoria')==='memory test');await clearAllMemory();ok('app memory reset also clears learned translations, Puter pricing and measured generation costs',cachedSearchTranslation('prova memoria')===''&&!localStorage.getItem(PUTER_IMAGE_PRICING_CACHE_KEY)&&!localStorage.getItem(PUTER_IMAGE_ACTUAL_CACHE_KEY));
 results.push('ALL V7 CHECKS COMPLETE')
 }catch(e){results.push('ERROR '+e.stack)}const pre=document.createElement('pre');pre.id='v7Results';pre.style='position:fixed;inset:10px;background:white;z-index:9999;overflow:auto;padding:20px';pre.textContent=results.join('\n');document.body.append(pre);pre.scrollTop=pre.scrollHeight;document.title=results.at(-1)}
runV7Tests();
