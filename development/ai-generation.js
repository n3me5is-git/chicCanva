// Curated Puter image generation profiles. Prices are published per-image base
// prices from the Puter model catalog; Puter exposes usage, not a quote API.
const AI_IMAGE_PROFILES=Object.freeze([
 {id:'gpt-image-2.5-flare',group:'openai-image-generation',label:'GPT Image 2.5 Flare',provider:'openai-image-generation',model:'openai/gpt-image-2.5-flare',providerModel:'gpt-image-2.5-flare',qualities:['low','medium','high'],defaultQuality:'low',resolutions:['min','816','1024','1536','2048'],defaultResolution:'1024',customResolution:true,usd:.00588,reference:true,arbitraryRatio:true,qualityUsd:{low:.00588,medium:.053,high:.211}},
 {id:'gpt-image-2',group:'openai-image-generation',label:'GPT Image 2',provider:'openai-image-generation',model:'openai/gpt-image-2',providerModel:'gpt-image-2',qualities:['low','medium'],defaultQuality:'low',resolutions:['min','816','1024','1536','2048'],defaultResolution:'1024',customResolution:true,usd:.0059,reference:true,arbitraryRatio:true,qualityUsd:{low:.0059,medium:.053}},
 {id:'grok-imagine-image',group:'xai',label:'Grok Imagine · Standard',model:'x-ai/grok-imagine-image',qualities:['standard'],defaultQuality:'standard',resolutions:['1k','2k'],defaultResolution:'1k',usd:.02,reference:true,referenceUsd:.002},
 {id:'grok-imagine-image-quality',group:'xai',label:'Grok Imagine · Quality',model:'x-ai/grok-imagine-image-quality',qualities:['standard'],defaultQuality:'standard',resolutions:['1k','2k'],defaultResolution:'1k',usd:.05,reference:true,referenceUsd:.002},
 {id:'byteplus/seedream-5-0-lite-260128',group:'other',label:'Seedream 5 Lite',model:'byteplus/seedream-5-0-lite-260128',qualities:['standard'],defaultQuality:'standard',usd:.035,reference:true},
 {id:'google/gemini-3.1-flash-lite-image',group:'other',label:'Gemini 3.1 Flash Lite Image',adapter:'gemini',model:'google/gemini-3.1-flash-lite-image',qualities:['1K'],defaultQuality:'1K',resolutions:['1K'],defaultResolution:'1K',ratios:['1:1','2:3','3:2','3:4','4:3','4:5','5:4','9:16','16:9','21:9'],usd:.0336,reference:true}
]);
const AI_REFERENCE_FACTORS=Object.freeze([.25,.5,.75,1]);
if(!DOCUMENT_FIELDS.includes('aiResolution'))DOCUMENT_FIELDS.push('aiResolution');
if(!DOCUMENT_FIELDS.includes('aiCustomResolution'))DOCUMENT_FIELDS.push('aiCustomResolution');
if(!DOCUMENT_FIELDS.includes('aiCustomWidth'))DOCUMENT_FIELDS.push('aiCustomWidth');
if(!DOCUMENT_FIELDS.includes('aiCustomHeight'))DOCUMENT_FIELDS.push('aiCustomHeight');
function aiProfile(){return AI_IMAGE_PROFILES.find(p=>p.id===$('aiModel')?.value)||AI_IMAGE_PROFILES[0]}
function aiReferenceFactor(){return AI_REFERENCE_FACTORS[clamp(Number($('aiReferenceScale')?.value??3),0,3)]||1}
const AI_TEXT_LANGUAGE_INSTRUCTION_IT='Se nell’immagine compaiono parole o frasi, usa la lingua del prompt scritto dall’utente, salvo una sua indicazione esplicita diversa.';
const AI_TEXT_LANGUAGE_INSTRUCTION_EN='If the image contains words or sentences, use the language of the user-written prompt unless the user explicitly requests another language.';
const AI_STYLES_IT={'Da prompt':'','Pagina da colorare':'Semplice pagina da colorare, contorni neri su fondo bianco, forme chiuse, nessun riempimento né ombreggiatura, ampie aree da colorare.','Contorni spessi':'Illustrazione con contorni neri spessi, forme molto semplici, interno bianco, adatta a una pagina da colorare per bambini.','Contorni delicati':'Contorni neri sottili e puliti, illustrazione semplice su fondo bianco, nessuna ombreggiatura, interni vuoti da colorare.','Fumetto a colori':'Fumetto semplice e colorato, contorni netti e marcati, colori piatti, pochi dettagli, fondo bianco.','Clipart educativa':'Clipart educativa, forme chiare e riconoscibili, colori piatti e allegri, fondo bianco pulito.','Pastello':'Illustrazione semplice dai colori pastello, forme arrotondate, dettagli minimi, fondo bianco.','Bianco e nero':'Illustrazione minimale in bianco e nero, contrasto deciso, forme pulite, nessun gradiente in scala di grigi.','Sticker':'Adesivo semplice, grazioso e colorato, bordo marcato, soggetto isolato, fondo bianco.','Geometrico':'Illustrazione geometrica semplice, grandi forme piatte e colorate, linee pulite, dettagli minimi.','Acquerello semplice':'Illustrazione ad acquerello semplice e delicata, palette luminosa limitata, soggetto isolato su fondo bianco, pochi dettagli.'};
function aiStyleInstruction(){const key=$('aiStyle')?.value;return (appLanguage==='en'?AI_STYLES:AI_STYLES_IT)[key]||''}
function aiTextLanguageInstruction(){return appLanguage==='en'?AI_TEXT_LANGUAGE_INSTRUCTION_EN:AI_TEXT_LANGUAGE_INSTRUCTION_IT}
function aiFullPrompt(forEstimate=false){
 let prompt=$('aiPrompt')?.value.trim()||'',style=aiStyleInstruction();
 if(style)prompt=[prompt,style].filter(Boolean).join('\n\n');
 const languageInstruction=aiTextLanguageInstruction();if(languageInstruction)prompt=[prompt,languageInstruction].filter(Boolean).join('\n\n');
 if(forEstimate&&$('aiChromaKey')?.checked&&typeof aiChromaInstruction==='function'&&!prompt.includes(aiChromaInstruction()))prompt=[prompt,aiChromaInstruction()].filter(Boolean).join('\n\n');
 return prompt
}
function aiUsdText(value){return 'US$ '+value.toLocaleString('it-IT',{minimumFractionDigits:value<.01?4:3,maximumFractionDigits:4})}
function aiEquivalentCredits(value){return value*2000}
function aiCreditText(value){const credits=aiEquivalentCredits(value),digits=credits<1?3:2;return credits.toLocaleString('it-IT',{minimumFractionDigits:digits,maximumFractionDigits:3})+' crediti'}
function aiSelectedRatio(){const value=$('aiRatio')?.value||'1:1';if(value!=='page')return value;return Math.max(1,currentPage?.widthMm||210)+':'+Math.max(1,currentPage?.heightMm||297)}
const OPENAI_IMAGE_LIMITS=Object.freeze({minPixels:655360,maxPixels:8294400,maxEdge:3840,maxAspect:3,multiple:16});
let aiLastValidOutput=null,aiOutputWarningTimer=0;
function aiRatioValues(ratio=aiSelectedRatio()){const parts=String(ratio).split(':').map(Number),w=parts[0],h=parts[1];return Number.isFinite(w)&&Number.isFinite(h)&&w>0&&h>0?{w,h,aspect:Math.max(w,h)/Math.min(w,h),landscape:w>=h}:null}
function aiRound16(value){return Math.ceil(value/OPENAI_IMAGE_LIMITS.multiple)*OPENAI_IMAGE_LIMITS.multiple}
function aiOpenAiDimensions(ratio=aiSelectedRatio(),resolution='min'){
 const values=aiRatioValues(ratio);if(!values)return{valid:false,reason:'Proporzioni non valide.'};if(values.aspect>OPENAI_IMAGE_LIMITS.maxAspect)return{valid:false,reason:'OpenAI accetta proporzioni comprese tra 1:3 e 3:1.'};
 let shortEdge=resolution==='min'?aiRound16(Math.sqrt(OPENAI_IMAGE_LIMITS.minPixels/values.aspect)):aiRound16(Number(resolution));if(!Number.isFinite(shortEdge)||shortEdge<16)return{valid:false,reason:'Risoluzione non valida.'};const longEdge=aiRound16(shortEdge*values.aspect),width=values.landscape?longEdge:shortEdge,height=values.landscape?shortEdge:longEdge,pixels=width*height;
 if(width>OPENAI_IMAGE_LIMITS.maxEdge||height>OPENAI_IMAGE_LIMITS.maxEdge)return{valid:false,reason:'Questa combinazione supera il lato massimo OpenAI di 3840 px.'};if(pixels>OPENAI_IMAGE_LIMITS.maxPixels)return{valid:false,reason:'Questa combinazione supera 8.294.400 pixel.'};if(pixels<OPENAI_IMAGE_LIMITS.minPixels)return{valid:false,reason:'Questa combinazione non raggiunge il minimo di 655.360 pixel.'};return{valid:true,width,height,pixels,shortEdge,longEdge,label:resolution==='min'?'minima':Math.round(shortEdge/102.4)/10+'K'}
}
function aiOpenAiCustomDimensions(width,height){
 width=Number(width);height=Number(height);if(!Number.isInteger(width)||!Number.isInteger(height)||width<16||height<16)return{valid:false,reason:'Larghezza e altezza devono essere numeri interi positivi.'};if(width%OPENAI_IMAGE_LIMITS.multiple||height%OPENAI_IMAGE_LIMITS.multiple)return{valid:false,reason:'Larghezza e altezza devono essere multipli di 16 px.'};const values=aiRatioValues(width+':'+height),pixels=width*height;if(values.aspect>OPENAI_IMAGE_LIMITS.maxAspect)return{valid:false,reason:'OpenAI accetta proporzioni comprese tra 1:3 e 3:1.'};if(width>OPENAI_IMAGE_LIMITS.maxEdge||height>OPENAI_IMAGE_LIMITS.maxEdge)return{valid:false,reason:'La risoluzione personalizzata supera il lato massimo OpenAI di 3840 px.'};if(pixels>OPENAI_IMAGE_LIMITS.maxPixels)return{valid:false,reason:'La risoluzione personalizzata supera 8.294.400 pixel.'};if(pixels<OPENAI_IMAGE_LIMITS.minPixels)return{valid:false,reason:'La risoluzione personalizzata non raggiunge il minimo di 655.360 pixel.'};return{valid:true,width,height,pixels,shortEdge:Math.min(width,height),longEdge:Math.max(width,height),label:'custom'}
}
function aiResolutionDimensions(profile,ratio=aiSelectedRatio()){
 const resolution=$('aiResolution')?.value||profile.defaultResolution||'1024';if(profile.group==='openai-image-generation')return $('aiCustomResolution')?.checked?aiOpenAiCustomDimensions($('aiCustomWidth')?.value,$('aiCustomHeight')?.value):aiOpenAiDimensions(ratio,resolution);const parts=ratio.split(':').map(Number),rw=parts[0]||1,rh=parts[1]||1,kMatch=String(resolution).match(/^(\d+(?:\.\d+)?)k$/i),longEdge=kMatch?Math.round(Number(kMatch[1])*1024):(parseInt(resolution,10)||1024);
 if(profile.adapter==='gemini'){const sizes={'1:1':[1024,1024],'2:3':[848,1264],'3:2':[1264,848],'3:4':[896,1200],'4:3':[1200,896],'4:5':[928,1152],'5:4':[1152,928],'9:16':[768,1376],'16:9':[1376,768],'21:9':[1584,672]},size=sizes[ratio]||sizes['1:1'];return{width:size[0],height:size[1],label:'1K'}}
 if(rw>=rh)return{width:longEdge,height:Math.max(64,Math.round(longEdge*rh/rw/16)*16),label:resolution.toUpperCase()};
 return{width:Math.max(64,Math.round(longEdge*rw/rh/16)*16),height:longEdge,label:resolution.toUpperCase()}
}
function aiReferenceEstimate(profile,width,height){if(!state.aiReference)return 0;if(profile.referenceUsd)return profile.referenceUsd*1.1;if(profile.group==='openai-image-generation'){const megapixels=Math.max(.05,width*height/1000000),estimatedInputUnits=20+32*megapixels,estimatedCredits=estimatedInputUnits*.01*1.15;return estimatedCredits/2000}return profile.usd*.25*1.15}
function aiOpenAiOutputEstimate(profile,quality,dimensions){
 const width=dimensions.width,height=dimensions.height,short=Math.min(width,height),long=Math.max(width,height),aspect=long/short,key=short+'x'+long,reviewed={'608x1088':96,'672x1584':112,'688x976':118,'768x1376':128,'816x816':171,'848x1264':184,'896x1200':190,'928x1152':192,'1024x1024':196,'1200x1200':256,'1264x1264':278,'1376x1376':326,'1456x2048':440,'1536x1536':392,'2048x2048':620,'2048x3840':870};let lowTokens=reviewed[key],confidence=lowTokens?'high':'medium';
 if(!lowTokens){
  const samples=Object.entries(reviewed).map(([size,tokens])=>{const[s,l]=size.split('x').map(Number);return{s,l,aspect:l/s,tokens,distance:Math.abs(Math.log(short/s))*.8+Math.abs(Math.log(aspect/(l/s)))*1.35}}).sort((a,b)=>a.distance-b.distance).slice(0,4),outside=short<608||short>2048||aspect>2.36;
  let weighted=0,weights=0;for(const sample of samples){const scale=Math.pow(short/sample.s,1.05)*Math.pow(aspect/sample.aspect,.12),weight=1/Math.max(.025,sample.distance);weighted+=sample.tokens*scale*weight;weights+=weight}lowTokens=weighted/weights;confidence=outside?'low':'medium'
 }
 const multiplier=(profile.qualityUsd?.[quality]||profile.qualityUsd?.low||profile.usd)/(profile.qualityUsd?.low||profile.usd),tokens=Math.max(1,Math.round(lowTokens*multiplier));return{tokens,usd:tokens*30/1000000,confidence}
}
function aiEstimateBreakdown(){
 const profile=aiProfile(),quality=$('aiQuality')?.value||profile.defaultQuality,prompt=aiFullPrompt(true),tokens=Math.max(1,Math.ceil(prompt.length/4)),ref=state.aiReference,factor=aiReferenceFactor(),ratio=aiSelectedRatio(),dimensions=aiResolutionDimensions(profile,ratio),resolutionFactor=profile.provider==='together'?dimensions.width*dimensions.height/(1024*1024):1,openAiOutput=profile.group==='openai-image-generation'&&dimensions.valid!==false?aiOpenAiOutputEstimate(profile,quality,dimensions):null,outputUsd=openAiOutput?.usd??((profile.qualityUsd?.[quality]||profile.usd)*resolutionFactor),promptUsd=profile.group==='openai-image-generation'?tokens/1000000*5:0;
 const referenceUsd=ref?aiReferenceEstimate(profile,Math.max(1,Math.round((ref.width||0)*factor)),Math.max(1,Math.round((ref.height||0)*factor))):0,usd=outputUsd+promptUsd+referenceUsd;return{profile,quality,prompt,tokens,ref,factor,ratio,dimensions,outputUsd,promptUsd,referenceUsd,usd}
}
function updateAiEstimate(){
 const box=$('aiCostEstimate');if(!box)return;const{tokens,ref,factor,dimensions,outputUsd,promptUsd,referenceUsd,usd}=aiEstimateBreakdown();
 let details=dimensions.valid===false?'output non valido':('output ~'+dimensions.width+' × '+dimensions.height+' px · prompt complessivo ~'+tokens+' token');
 if(ref){const w=Math.max(1,Math.round((ref.width||0)*factor)),h=Math.max(1,Math.round((ref.height||0)*factor));details+=' · riferimento '+w+' × '+h+' px ('+String(factor).replace('.',',')+'×)'}
 const parts=['output ~'+aiCreditText(outputUsd)];if(promptUsd)parts.push('testo ~'+aiCreditText(promptUsd));if(referenceUsd)parts.push('riferimento ~'+aiCreditText(referenceUsd));
 box.innerHTML='<strong>Stima consumo Puter</strong><span>~ '+aiCreditText(usd)+' · '+aiUsdText(usd)+'<small class="estimate-detail">'+parts.join(' · ')+'<br>'+details+' · stima calibrata sui Credits Puter osservati; il consumo effettivo resta quello mostrato sopra</small></span>'
}
populateAiModels=function(){
 const group=$('aiProvider').value,sel=$('aiModel'),previous=sel.value,items=AI_IMAGE_PROFILES.filter(p=>p.group===group);sel.replaceChildren();
 for(const p of items){const option=document.createElement('option');option.value=p.id;option.textContent=p.label;sel.append(option)}
 sel.value=items.some(p=>p.id===previous)?previous:items[0]?.id||AI_IMAGE_PROFILES[0].id;syncAiQualityOptions();syncAiOutputOptions();syncReferenceUI();updateAiEstimate()
};
syncAiQualityOptions=function(){
 const profile=aiProfile(),sel=$('aiQuality'),previous=sel.value;sel.replaceChildren();
 for(const quality of profile.qualities){const option=document.createElement('option');option.value=quality;option.textContent=quality==='standard'?'Standard':quality[0].toUpperCase()+quality.slice(1);sel.append(option)}
 sel.value=profile.qualities.includes(previous)?previous:profile.defaultQuality;sel.disabled=profile.qualities.length===1;updateAiEstimate()
};
function syncAiOutputOptions(){
 const profile=aiProfile(),resolution=$('aiResolution'),resolutionWrap=$('aiResolutionWrap'),customWrap=$('aiCustomResolutionWrap'),custom=$('aiCustomResolution'),previousResolution=resolution?.value;if(resolution){resolution.replaceChildren();for(const value of profile.resolutions||[]){let label=/^\d+$/.test(value)?(Number(value)/1024).toLocaleString('it-IT',{maximumFractionDigits:1})+'K':value.toUpperCase();if(profile.customResolution){label=value==='min'?'Minima valida':value==='816'?'≈ 0,8K lato corto':value==='1024'?'1K lato corto':value==='1536'?'1,5K lato corto':value==='2048'?'2K lato corto':label}resolution.add(new Option(label,value))}resolution.value=(profile.resolutions||[]).includes(previousResolution)?previousResolution:(profile.defaultResolution||profile.resolutions?.[0]||'')}
 customWrap?.classList.toggle('hidden',!profile.customResolution);if(!profile.customResolution&&custom)custom.checked=false;resolutionWrap?.classList.toggle('hidden',!profile.resolutions?.length);if($('aiResolutionLabel'))$('aiResolutionLabel').textContent=profile.customResolution?'Risoluzione · lato corto':'Risoluzione';
 const ratio=$('aiRatio'),previousRatio=ratio.value,items=profile.ratios?[...profile.ratios]:(profile.group==='xai'?['1:1']:['1:1','4:3','3:4','16:9','9:16']);if((profile.arbitraryRatio||profile.provider==='together')&&!items.includes('page'))items.push('page');ratio.replaceChildren(...items.map(value=>new Option(value==='page'?'Come la pagina':value,value)));ratio.value=items.includes(previousRatio)?previousRatio:'1:1';
 const customFields=$('aiCustomResolutionFields');customFields?.classList.toggle('hidden',!profile.customResolution||!custom?.checked);if(resolution)resolution.disabled=!!custom?.checked;if(ratio)ratio.disabled=profile.group==='xai'||!!custom?.checked;
 if(profile.group==='openai-image-generation'&&aiResolutionDimensions(profile).valid===false){ratio.value='1:1';if(resolution)resolution.value=profile.defaultResolution||'1024';if(custom)custom.checked=false;if($('aiCustomWidth'))$('aiCustomWidth').value='1024';if($('aiCustomHeight'))$('aiCustomHeight').value='1024';customFields?.classList.add('hidden');resolution.disabled=false;ratio.disabled=false}
 aiLastValidOutput={ratio:ratio.value,resolution:resolution?.value||'',custom:!!custom?.checked,width:$('aiCustomWidth')?.value||'1024',height:$('aiCustomHeight')?.value||'1024'};validateAiOutputSelection(false);updateAiOutputHelp();updateAiEstimate()
}
function showAiOutputWarning(message){const warning=$('aiOutputWarning');if(!warning)return;clearTimeout(aiOutputWarningTimer);warning.textContent=message;warning.classList.remove('hidden');aiOutputWarningTimer=setTimeout(()=>warning.classList.add('hidden'),5000)}
function updateAiOutputHelp(){const profile=aiProfile(),help=$('aiOutputHelp');if(!help)return;if(profile.group==='openai-image-generation'){const dimensions=aiResolutionDimensions(profile);help.textContent=(dimensions.valid===false?dimensions.reason:'Output previsto '+dimensions.width+' × '+dimensions.height+' px. ')+'Qualità e risoluzione sono indipendenti; i lati sono multipli di 16, rapporto massimo 3:1 e limite 3840 px / 8.294.400 pixel.'}else if(profile.adapter==='gemini')help.textContent='Gemini Flash Lite supporta i rapporti elencati e un solo livello output 1K; Puter non espone un livello qualitativo separato per questo modello.';else help.textContent=profile.group==='xai'?'Puter espone per Grok il livello 1K/2K, ma al momento non documenta l’inoltro dell’aspect ratio tramite txt2img().':'Il modello espone soltanto le opzioni supportate dal relativo provider.'}
function syncAiCustomResolutionControls(){const profile=aiProfile(),enabled=profile.customResolution&&!!$('aiCustomResolution')?.checked;$('aiCustomResolutionFields')?.classList.toggle('hidden',!enabled);if($('aiResolution'))$('aiResolution').disabled=enabled;if($('aiRatio'))$('aiRatio').disabled=profile.group==='xai'||enabled}
function validateAiOutputSelection(notify=true){const profile=aiProfile();if(profile.group!=='openai-image-generation'){aiLastValidOutput={ratio:$('aiRatio').value,resolution:$('aiResolution').value,custom:!!$('aiCustomResolution')?.checked,width:$('aiCustomWidth')?.value||'1024',height:$('aiCustomHeight')?.value||'1024'};return true}const dimensions=aiResolutionDimensions(profile);if(dimensions.valid===false){if(aiLastValidOutput){$('aiRatio').value=aiLastValidOutput.ratio;$('aiResolution').value=aiLastValidOutput.resolution;$('aiCustomResolution').checked=aiLastValidOutput.custom;$('aiCustomWidth').value=aiLastValidOutput.width;$('aiCustomHeight').value=aiLastValidOutput.height;syncAiCustomResolutionControls()}if(notify)showAiOutputWarning(dimensions.reason+' È stata ripristinata l’ultima combinazione valida.');updateAiOutputHelp();updateAiEstimate();return false}aiLastValidOutput={ratio:$('aiRatio').value,resolution:$('aiResolution').value,custom:!!$('aiCustomResolution')?.checked,width:$('aiCustomWidth').value,height:$('aiCustomHeight').value};syncAiCustomResolutionControls();updateAiOutputHelp();return true}
refreshAiModels=function(){populateAiModels();$('aiStatus').textContent='Catalogo curato chicCanva aggiornato.'};
referenceSupported=function(){return !!aiProfile().reference};
syncReferenceUI=function(){
 const ref=state.aiReference,profile=aiProfile(),factor=aiReferenceFactor(),scalePanel=$('aiReferenceScalePanel');$('aiReferencePreview').classList.toggle('hidden',!ref);scalePanel?.classList.toggle('hidden',!ref);
 if(ref){$('aiReferenceImg').src=ref.dataUrl;$('aiReferenceName').textContent=ref.name;const w=Math.max(1,Math.round((ref.width||0)*factor)),h=Math.max(1,Math.round((ref.height||0)*factor));$('aiReferenceScaleLabel').textContent=String(factor).replace('.',',')+'×';$('aiReferenceScaleInfo').textContent='Invio: '+w+' × '+h+' px. L’originale resta invariato; la copia ridotta esiste solo durante la richiesta.'}else $('aiReferenceImg').removeAttribute('src');
 $('aiReferenceStatus').textContent=ref&&!profile.reference?'Questo modello non dichiara un input immagine compatibile: rimuovi il riferimento o scegli un altro modello.':ref?'Riferimento pronto · descrivi nel prompt cosa mantenere o cambiare.':'Allega una foto o un disegno; senza allegato la generazione parte dal solo testo.';
 if(!$('generateAiBtn').querySelector('.spinner'))$('generateAiBtn').disabled=!!ref&&!profile.reference;updateAiEstimate()
};
async function scaledAiReference(reference,factor){
 if(!reference||factor>=.999)return reference;const image=new Image();image.src=reference.dataUrl;await image.decode();const canvas=document.createElement('canvas');canvas.width=Math.max(1,Math.round(image.naturalWidth*factor));canvas.height=Math.max(1,Math.round(image.naturalHeight*factor));const context=canvas.getContext('2d',{alpha:true});context.imageSmoothingEnabled=true;context.imageSmoothingQuality='high';context.drawImage(image,0,0,canvas.width,canvas.height);const dataUrl=canvas.toDataURL('image/png');const result={...reference,dataUrl,mime:'image/png',width:canvas.width,height:canvas.height};canvas.width=canvas.height=1;image.src='';return result
}
function aiRequestOptions(profile,quality,ratio,reference){
 // Keep the canonical Puter model ID unless a provider-specific adapter is
 // required for documented controls (currently only OpenAI exact dimensions).
 const opts={model:profile.providerModel||profile.model};if(profile.provider)opts.provider=profile.provider;
 if(profile.group==='openai-image-generation'){opts.quality=quality;const size=aiResolutionDimensions(profile,ratio);opts.ratio={w:size.width,h:size.height}}
 else if(profile.group==='xai'){const tier=$('aiResolution')?.value;opts.quality=profile.resolutions?.includes(tier)?tier:(profile.defaultResolution||'1k')}
 else if(profile.adapter==='gemini'){const values=aiRatioValues(ratio)||{w:1,h:1};opts.ratio={w:values.w,h:values.h};opts.quality='1K'}
 if(profile.disableSafety)opts.disable_safety_checker=true;
 if(reference){opts.input_images=[reference.dataUrl];opts.input_image_mime_type=reference.mime}
 return opts
}
async function aiGeneratedImageDataUrl(imageResult){
 if(imageResult instanceof Blob)return dataUrlFromBlob(imageResult);
 if(imageResult instanceof HTMLCanvasElement)return imageResult.toDataURL('image/png');
 const element=imageResult instanceof HTMLImageElement?imageResult:null,source=aiGeneratedSource(imageResult);if(!source)throw new Error('Puter non ha restituito un’immagine leggibile');
 if(source.startsWith('data:'))return source;
 // Some providers return an already decoded image element backed by a URL that
 // is not downloadable cross-origin. Drawing that element first avoids a
 // second network request whenever its bitmap is origin-clean.
 if(element){try{if(!element.complete||!element.naturalWidth)await element.decode();const surface=document.createElement('canvas');surface.width=element.naturalWidth||element.width;surface.height=element.naturalHeight||element.height;surface.getContext('2d',{alpha:true}).drawImage(element,0,0);const dataUrl=surface.toDataURL('image/png');surface.width=surface.height=1;return dataUrl}catch(error){/* continue through the download fallbacks */}}
 const errors=[];
 try{const response=await aiTimedFetch(source,15000);if(!response.ok)throw new Error('HTTP '+response.status);return await dataUrlFromBlob(await response.blob())}catch(error){errors.push(error)}
 if(isLocalChicServer?.()){try{const response=await aiTimedFetch('/__chiccanva_image_proxy?url='+encodeURIComponent(source),22000,{credentials:'same-origin'});if(!response.ok)throw new Error((await response.text()).trim()||'HTTP '+response.status);return await dataUrlFromBlob(await response.blob())}catch(error){errors.push(error)}}
 if(window.puter?.net?.fetch){try{const timeout=new Promise((_,reject)=>setTimeout(()=>reject(new Error('timeout del relay Puter')),22000)),response=await Promise.race([puter.net.fetch(source,{method:'GET'}),timeout]);if(!response.ok)throw new Error('HTTP '+response.status);return await dataUrlFromBlob(await response.blob())}catch(error){errors.push(error)}}
 throw new Error('download del risultato non riuscito: '+(errors.at(-1)?.message||'URL non accessibile'))
}
function aiGeneratedSource(result){
 if(typeof result==='string')return result;
 const candidates=[result?.src,result?.url,result?.data,result?.image_url,result?.image?.src,result?.images?.[0]?.src,result?.images?.[0]?.url,result?.images?.[0]];
 return candidates.find(value=>typeof value==='string'&&value.trim())||''
}
async function aiTimedFetch(url,timeoutMs,options={}){const controller=new AbortController(),timer=setTimeout(()=>controller.abort(),timeoutMs);try{return await fetch(url,{...options,signal:controller.signal})}finally{clearTimeout(timer)}}
let aiPendingGenerated=null;
function aiSetPendingPreview(pending,error){
 aiPendingGenerated=pending;const source=aiGeneratedSource(pending.result),box=$('aiGeneratedFallback'),message='Il modello ha completato la generazione, ma il browser non è riuscito a importare automaticamente il file'+(error?.message?' ('+error.message+').':'.');
 $('aiGeneratedFallbackText').textContent=message+' Puoi riprovare, scaricarlo o aprirlo senza perdere il risultato.';$('aiGeneratedFallbackImage').src=source;$('aiGeneratedDialogImage').src=source;$('aiGeneratedDialogMeta').textContent=pending.profile.label+' · '+pending.profile.model;box.classList.remove('hidden')
}
function aiClearPendingPreview(){aiPendingGenerated=null;$('aiGeneratedFallback').classList.add('hidden');for(const id of ['aiGeneratedFallbackImage','aiGeneratedDialogImage'])$(id).removeAttribute('src');if($('aiGeneratedDialog').open)$('aiGeneratedDialog').close()}
async function aiInsertGeneratedDataUrl(dataUrl,pending){
 const{profile,quality,ratio,prompt,sourceReference,factor,targetProjectId,targetPageId,estimate,resolution}=pending,id=uid('asset'),asset={id,dataUrl,originalDataUrl:dataUrl,mime:'image/png',name:'AI '+profile.label,kind:'ai',source:'Puter txt2img',meta:{provider:profile.provider||'auto',model:profile.model,profile:profile.id,quality,resolution:resolution||null,ratio,prompt,referenceName:sourceReference?.name||null,referenceScale:sourceReference?factor:null,estimatedUsd:estimate.usd,estimatedCredits:aiEquivalentCredits(estimate.usd)}};
 const targetRecord=projects.find(project=>project.id===targetProjectId),destination=activeProjectId===targetProjectId?state.pages.custom.find(page=>page.id===targetPageId):targetRecord?.payload?.pages?.custom?.find(page=>page.id===targetPageId);if(!destination)throw new Error('Il progetto di destinazione non è più aperto');
 if(activeProjectId===targetProjectId){state.assets[id]=asset;updateAssetCount()}else{targetRecord.payload.assets=targetRecord.payload.assets||{};targetRecord.payload.assets[id]=asset}
 if(currentPage===destination&&!loadingPage)await addImageAsset(id,'ai');else{const image=new Image();image.src=dataUrl;await image.decode();const scale=Math.min(1,destination.widthMm*PX_PER_MM*.55/image.naturalWidth,destination.heightMm*PX_PER_MM*.55/image.naturalHeight);destination.objects.push({id:uid('obj'),objectType:'ai',assetId:id,left:destination.widthMm*PX_PER_MM/2,top:destination.heightMm*PX_PER_MM/2,originX:'center',originY:'center',scaleX:scale,scaleY:scale,width:image.naturalWidth,height:image.naturalHeight,angle:0,opacity:1});if(targetRecord)targetRecord.payload=cloneData(targetRecord.payload)}
 scheduleAutosave?.();return destination
}
async function aiRetryPendingInsert(){
 if(!aiPendingGenerated)return;const buttons=[$('aiGeneratedRetryBtn'),$('aiGeneratedDialogRetry')];buttons.forEach(button=>button.disabled=true);$('aiGeneratedDialogStatus').textContent='Recupero e inserimento nel progetto…';try{const dataUrl=await aiGeneratedImageDataUrl(aiPendingGenerated.result),destination=await aiInsertGeneratedDataUrl(dataUrl,aiPendingGenerated);$('aiStatus').textContent='Immagine recuperata e inserita in '+destination.name+'.';toast('Immagine inserita');aiClearPendingPreview()}catch(error){$('aiGeneratedDialogStatus').textContent='Il recupero non è riuscito: '+(error?.message||error);$('aiGeneratedFallbackText').textContent='Il risultato resta disponibile. Il recupero non è riuscito: '+(error?.message||error)}finally{buttons.forEach(button=>button.disabled=false)}}
async function aiDownloadPending(){
 if(!aiPendingGenerated)return;const status=$('aiGeneratedDialogStatus');status.textContent='Preparazione del download…';try{const dataUrl=await aiGeneratedImageDataUrl(aiPendingGenerated.result),blob=await(await fetch(dataUrl)).blob();download(cleanExportName('chicCanva '+aiPendingGenerated.profile.label,'immagine-ai')+'.png',blob);status.textContent='Download avviato.'}catch(error){status.textContent='Download automatico non disponibile. Usa “Apri in nuova scheda” e salva o copia l’immagine dal browser.'}
}
function aiOpenPending(){const source=aiPendingGenerated&&aiGeneratedSource(aiPendingGenerated.result);if(source)window.open(source,'_blank','noopener')}
function aiPuterGenerationError(error,profile){
 const raw=String(error?.message||error||'Errore sconosciuto');
 if(/model_not_available|Unable to access non-serverless model/i.test(raw)){
  const routed=/black-forest-labs\/FLUX\.1-schnell/i.test(raw)?' Puter lo ha instradato internamente verso FLUX.1-schnell, che il provider ha rifiutato.':'';
  return 'Il modello '+profile.label+' è temporaneamente non disponibile tramite Puter.'+routed+' La richiesta originale usava '+profile.model+'; non è stato avviato alcun modello alternativo.'
 }
 return raw
}
generateAi=async function(){
 if(location.protocol==='file:'){toast('Apri tramite webserver per Puter');return}const raw=$('aiPrompt').value.trim();if(!raw){toast('Scrivi cosa vuoi creare');return}const profile=aiProfile();if(state.aiReference&&!profile.reference){toast('Il modello scelto non supporta il riferimento');return}
 if(!validateAiOutputSelection())return;
 const btn=$('generateAiBtn'),status=$('aiStatus'),targetProjectId=activeProjectId,targetPageId=currentPage.id,sourceReference=state.aiReference?structuredClone(state.aiReference):null;let usageBefore=null,usageSignature=null;btn.disabled=true;btn.innerHTML='<span class="spinner"></span>Generazione…';
 try{await ensurePuter();const quality=$('aiQuality').value,ratio=aiSelectedRatio(),prompt=aiFullPrompt(false),factor=aiReferenceFactor(),resolution=$('aiResolution')?.value||null;usageSignature=typeof aiEstimateSignature==='function'?aiEstimateSignature():null;try{usageBefore=typeof readPuterAppUsageCredits==='function'?await readPuterAppUsageCredits():null}catch{}status.textContent=(sourceReference&&factor<1?'Preparazione della copia ridotta del riferimento…':sourceReference?'Generazione con immagine di riferimento…':'Generazione in corso…')+' · '+profile.model;const reference=await scaledAiReference(sourceReference,factor),opts=aiRequestOptions(profile,quality,ratio,reference),imageResult=await puter.ai.txt2img(prompt,opts),pending={result:imageResult,profile,quality,ratio,resolution,prompt,sourceReference,factor,targetProjectId,targetPageId,estimate:aiEstimateBreakdown()};if(typeof recordPuterGenerationUsage==='function')recordPuterGenerationUsage(usageBefore,usageSignature).catch(console.warn);status.textContent='Immagine generata · inserimento nel progetto…';let dataUrl;
  try{dataUrl=await aiGeneratedImageDataUrl(imageResult)}catch(importError){console.warn('Generazione completata; import automatico non riuscito',importError);aiSetPendingPreview(pending,importError);status.textContent='Immagine generata con '+profile.label+'; usa l’anteprima qui sotto per recuperarla.';return}
  const destination=await aiInsertGeneratedDataUrl(dataUrl,pending);aiClearPendingPreview();status.textContent='Immagine generata con '+profile.label+' e inserita in '+destination.name+'.'
 }catch(error){console.error(error);status.textContent='Errore Puter: '+aiPuterGenerationError(error,profile);toast('Generazione non riuscita')}finally{btn.innerHTML='Genera e inserisci';btn.disabled=false;syncReferenceUI();schedulePuterUsageRefresh?.()}
};
const baseRestoreAiFields=restoreDocumentFields;
restoreDocumentFields=function(fields={}){baseRestoreAiFields(fields);const group=AI_IMAGE_PROFILES.some(p=>p.group===$('aiProvider').value)?$('aiProvider').value:'openai-image-generation';$('aiProvider').value=group;populateAiModels();if(fields.aiModel&&AI_IMAGE_PROFILES.some(p=>p.id===fields.aiModel&&p.group===group)){$('aiModel').value=fields.aiModel;syncAiQualityOptions();syncAiOutputOptions()}if(fields.aiQuality&&aiProfile().qualities.includes(fields.aiQuality))$('aiQuality').value=fields.aiQuality;if(fields.aiResolution&&aiProfile().resolutions?.includes(fields.aiResolution))$('aiResolution').value=fields.aiResolution;if(fields.aiCustomWidth)$('aiCustomWidth').value=fields.aiCustomWidth;if(fields.aiCustomHeight)$('aiCustomHeight').value=fields.aiCustomHeight;if(fields.aiCustomResolution!=null)$('aiCustomResolution').checked=!!fields.aiCustomResolution;if(fields.aiReferenceScale!=null)$('aiReferenceScale').value=fields.aiReferenceScale;syncAiOutputOptions();syncReferenceUI()};
const aiGenerationBaseInit=init;
init=async function(){await aiGenerationBaseInit();$('aiModel').onchange=()=>{syncAiQualityOptions();syncAiOutputOptions();syncReferenceUI();scheduleAutosave?.()};$('aiQuality').onchange=()=>{updateAiEstimate();scheduleAutosave?.()};$('aiResolution').onchange=()=>{validateAiOutputSelection();updateAiEstimate();scheduleAutosave?.()};$('aiCustomResolution').onchange=()=>{syncAiCustomResolutionControls();validateAiOutputSelection();updateAiEstimate();scheduleAutosave?.()};for(const id of ['aiCustomWidth','aiCustomHeight']){$(id).oninput=()=>{updateAiOutputHelp();updateAiEstimate()};$(id).onchange=()=>{validateAiOutputSelection();scheduleAutosave?.()}}$('aiRatio').onchange=()=>{validateAiOutputSelection();updateAiEstimate();scheduleAutosave?.()};$('aiReferenceScale').oninput=()=>syncReferenceUI();$('aiReferenceScale').onchange=()=>scheduleAutosave?.();for(const id of ['aiStyle','aiChromaKey'])$(id).addEventListener('change',updateAiEstimate);$('aiPrompt').addEventListener('input',updateAiEstimate);$('showRawAiPrompt').onclick=()=>{$('rawAiPromptText').textContent=aiFullPrompt(true)||'Il prompt è vuoto.';$('rawAiPromptDialog').showModal()};$('closeRawAiPrompt').onclick=()=>$('rawAiPromptDialog').close();$('aiGeneratedFallbackPreview').onclick=()=>{$('aiGeneratedDialogStatus').textContent='';$('aiGeneratedDialog').showModal()};$('aiGeneratedDialogClose').onclick=()=>$('aiGeneratedDialog').close();$('aiGeneratedRetryBtn').onclick=$('aiGeneratedDialogRetry').onclick=aiRetryPendingInsert;$('aiGeneratedDownloadBtn').onclick=$('aiGeneratedDialogDownload').onclick=aiDownloadPending;$('aiGeneratedOpenBtn').onclick=$('aiGeneratedDialogOpen').onclick=aiOpenPending;populateAiModels();updateAiEstimate()};
